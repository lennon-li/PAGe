% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested_loso.R
\name{nested_loso_cv}
\alias{nested_loso_cv}
\title{Nested M1 → M2 leave-one-season-out cross-validation}
\usage{
nested_loso_cv(
  allD,
  params,
  spec,
  test_seasons = NULL,
  exclude_seasons = NULL,
  horizons = c(1L, 2L),
  eval_window = 12L,
  k_deriv = 10L,
  k_ref = 25L,
  n_weeks = 52L,
  ref_method = "fs",
  manual_labels = NULL,
  flag_args = list(p_thresh = 0.01, k1 = 0.4, k_c = 0.01, n_consec = 2L, min_window =
    10L, w_min = 21L, w_max = 21L, d2_relax = -0.01),
  allow_scale = NULL,
  use_ci = TRUE,
  buffer_weeks = 0L,
  min_obs = 4L,
  curvature_ratio = 1,
  temperature = 0.25,
  rise_weight = 1,
  trough_weight = 0.1,
  peak_decay = 0.3,
  slope_weight = 0.5,
  slope_window = 4L,
  dynamic_temp = TRUE,
  dynamic_temp_pivot = 10L,
  top_k = NULL,
  blend_alpha = 1,
  method = "REML",
  n_cores = parallel::detectCores() - 1L,
  skip_m1 = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{allD}{Data frame with all seasons.}

\item{params}{M0 detection parameters.}

\item{spec}{M2 hyperparameter spec object.}

\item{test_seasons}{Character vector of seasons to hold out.
If \code{NULL}, every season is tested.}

\item{exclude_seasons}{Character vector of seasons to exclude entirely.}

\item{horizons}{Integer vector of forecast horizons (default \code{c(1L, 2L)}).}

\item{eval_window}{Integer; max weeks post-ignition (default 12L).}

\item{k_deriv}{Integer; basis dim for derivatives (default 10L).}

\item{k_ref}{Integer; basis dim for reference curve (default 10L).}

\item{n_weeks}{Integer; reference curve period (default 52L).}

\item{manual_labels}{Optional manual ignition labels.}

\item{flag_args}{Named list forwarded to \code{flagIgnition()}.}

\item{allow_scale}{Passed to M1 walk-forward.}

\item{use_ci}{Logical; peak CI control (default TRUE).}

\item{buffer_weeks}{Integer; peak buffer (default 0L).}

\item{min_obs}{Integer; minimum rows for alignment (default 4L).}

\item{curvature_ratio}{Numeric; delta curvature gate (default 1.0).}

\item{method}{GAM fitting method (default \code{"REML"}).}

\item{n_cores}{Integer; number of worker cores for M1 parallelism
(default \code{parallel::detectCores() - 1L}).}

\item{verbose}{Logical; print progress.}
}
\value{
A named list (same structure as \code{loso_m1_m2_joint()}):
  \describe{
    \item{scores}{Tibble with one row per test season: season, n,
      mean_nll, brier, rmse_p.}
    \item{predictions}{Tibble of all per-observation predictions
      across test seasons.}
    \item{m1_preds}{Named list of M1 test predictions per season.}
    \item{folds}{Named list of fold objects for diagnostics.}
  }
}
\description{
For each held-out test season, builds a leakage-free reference curve
on training seasons, runs M1 walk-forward to generate stacking
features, trains M2, and evaluates on the test season. This is the
composable replacement for \code{loso_m1_m2_joint()} in
\code{m1_m2_bridge.R}.
}
