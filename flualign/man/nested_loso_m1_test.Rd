% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2_nested_loso.R
\name{nested_loso_m1_test}
\alias{nested_loso_m1_test}
\title{Run M1 walk-forward on the held-out test season}
\usage{
nested_loso_m1_test(
  allD,
  fold,
  params,
  horizons = c(1L, 2L),
  allow_scale = NULL,
  use_ci = TRUE,
  buffer_weeks = 0L,
  min_obs = 4L,
  curvature_ratio = 1,
  temperature = 0.25,
  rise_weight = 1,
  trough_weight = 0.1,
  peak_decay = 0.3,
  slope_weight = 0.5,
  slope_window = 4L,
  dynamic_temp = TRUE,
  dynamic_temp_pivot = 10L,
  top_k = NULL,
  blend_alpha = 1,
  verbose = TRUE
)
}
\arguments{
\item{allD}{Full multi-season data frame.}

\item{fold}{Output of \code{nested_loso_build_fold()}.}

\item{params}{M0 detection parameters.}

\item{horizons}{Integer vector of forecast horizons (default \code{c(1L, 2L)}).}

\item{allow_scale}{Passed to \code{m1_walkforward_predictions()}.}

\item{use_ci}{Logical; peak CI control (default TRUE).}

\item{buffer_weeks}{Integer; peak buffer (default 0L).}

\item{min_obs}{Integer; minimum rows for alignment (default 4L).}

\item{curvature_ratio}{Numeric; delta curvature gate (default 1.0).}

\item{verbose}{Logical; print progress.}
}
\value{
Tibble of M1 walk-forward predictions for the test season,
  or a zero-row tibble if no ignition detected.
}
\description{
Generates M1 stacking features for the test season using the fold's
reference curve (which was fitted without the test season).
}
