% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prospective_training.R
\name{tune_stage2_loso_shift_template}
\alias{tune_stage2_loso_shift_template}
\title{Tune Stage-2 over (delta,K,k_f,alpha_state) with a fixed model structure}
\usage{
tune_stage2_loso_shift_template(
  dat,
  template_df,
  spec_base = NULL,
  testSeason = NULL,
  shift_grid = -3:3,
  ign_hat_df = NULL,
  pre_buffer = 1L,
  K_grid = 2:6,
  k_f_grid = c(6L, 8L, 10L),
  alpha_grid = c(0.15, 0.25, 0.35, 0.5),
  leads = c(1L, 2L),
  lambda_w_grid = 0,
  eval_window = NULL,
  num.cores = 8L,
  verbose = TRUE
)
}
\arguments{
\item{dat}{Multi-season input data.}

\item{template_df}{Template curve.}

\item{spec_base}{A spec that defines model structure (k's, template_mode, etc.).}

\item{shift_grid, K_grid, k_f_grid, alpha_grid}{Grids to tune.}

\item{ign_hat_df}{Optional ignition week estimates by season.}

\item{pre_buffer}{Weeks before ignition included.}

\item{leads}{Forecast horizons.}

\item{num.cores}{Parallel workers.}
}
\value{
list(results, best_by_season, best_overall)
}
\description{
This is a compatibility wrapper that replicates the QMD workflow, but internally
it expands a grid of specs and calls \code{tune_stage2_loso_specs()}.
}
\details{
If \code{spec_base} is NULL, it uses the **full model** (all terms on) as the default.
That means your Stage-2 now includes:
\code{s(logit_f_eff)} + \code{s(newWeek)} + \code{fs(newWeek,season_h)} + \code{z_ema, logN_now, d1, d2} + season RE.
}
