% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2_training.R
\name{m2_predict_one}
\alias{m2_predict_one}
\title{Predict M2 positivity from a fitted Stage-2 GAM (single row)}
\usage{
m2_predict_one(
  fit,
  ew,
  h,
  iWeek,
  anchorWeek,
  logit_f_eff,
  z_ema,
  dz_ema = 0,
  logit_spread = 0,
  logN_now,
  season_label = NULL,
  ex_terms = NULL,
  include_season_re = FALSE,
  soft_cap_fn = NULL,
  return_ci = FALSE,
  bias_logit = 0
)
}
\arguments{
\item{fit}{A fitted \code{mgcv::bam}/\code{gam} object.}

\item{ew}{Integer. Current evaluation week (weekF).}

\item{h}{Integer. Forecast horizon (1 or 2).}

\item{iWeek}{Integer. Locked ignition week.}

\item{anchorWeek}{Integer. Reference-curve anchor week.}

\item{logit_f_eff}{Numeric. logit(M1 predicted positivity at target week).}

\item{z_ema}{Numeric. EWMA of logit-observed positivity.}

\item{dz_ema}{Numeric. Rate of change of z_ema (z_ema[t] - z_ema[t-1]).}

\item{logN_now}{Numeric. log(N) at eval week.}

\item{season_label}{Character. Season label for newdata: the test/current
season name (used when the season is in the model's training data,
i.e. weekly-refit mode), or \code{NULL} to fall back to the first
historical level (frozen mode).}

\item{ex_terms}{Character vector. Terms to exclude from \code{predict()}
(e.g. exclude_newseason terms). Should NOT include \code{"s(season)"}
when the season is in the refit training data.}

\item{include_season_re}{Logical. If \code{TRUE}, the season random effect
is included in the prediction (weekly-refit mode). If \code{FALSE},
\code{"s(season)"} is appended to \code{ex_terms} (frozen mode).}

\item{soft_cap_fn}{Optional soft-cap function from \code{make_soft_cap_fn()}.}

\item{return_ci}{Logical. If \code{TRUE}, returns \code{m2_lo} and
\code{m2_hi} (±1.96 SE on the link scale).}
}
\value{
A named list with \code{m2_p} (and \code{m2_lo}, \code{m2_hi}
  if \code{return_ci = TRUE}), or \code{NULL} on prediction failure.
}
\description{
Shared prediction core used by all M2 code paths: LOSO frozen evaluation,
LOSO weekly-refit evaluation, and prospective deployment. By centralising
the newdata construction, factor-level alignment, season-RE handling,
soft-cap application, and CI logic in one place, we guarantee that
training-evaluation-deployment are fully consistent.
}
