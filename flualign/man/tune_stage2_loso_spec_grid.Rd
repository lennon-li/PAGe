% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_training.R
\name{tune_stage2_loso_spec_grid}
\alias{tune_stage2_loso_spec_grid}
\title{LOSO tuning over a spec grid with weighted scoring near ignition}
\usage{
tune_stage2_loso_spec_grid(
  alignedD_prosp,
  template_df,
  spec_grid,
  ignD = NULL,
  seasons = NULL,
  k_t = 10L,
  w_early = 2,
  exclude_newseason_terms = TRUE,
  nthreads = 1L,
  verbose = TRUE,
  parallel = TRUE,
  parallel_over = c("spec", "fold"),
  workers = NULL,
  strategy = c("auto", "multisession", "multicore", "sequential"),
  max_global_size_gb = 4
)
}
\arguments{
\item{alignedD_prosp}{Raw aligned dataset.}

\item{template_df}{Template reference df.}

\item{spec_grid}{Output of \code{expand_grid_specs()} (must include \code{$specs} and \code{$grid}).}

\item{ignD}{Optional ignition rule table (fallback).}

\item{seasons}{Optional subset of seasons to include. Must be >=3 for LOSO.}

\item{k_t}{Integer emphasized window length (weeks since ignition on TARGET week).}

\item{w_early}{Numeric multiplier for emphasized window.}

\item{exclude_newseason_terms}{If TRUE, exclude \code{spec$exclude_newseason} when predicting held-out season.}

\item{nthreads}{Threads for bam fits
#' Parallelization uses future.apply and works on Windows (multisession) and Linux/macOS (multicore).
Best practice: parallelize over "spec" and set mgcv::bam(nthreads=1) to avoid oversubscription.}

\item{verbose}{Logical.}

\item{parallel}{Logical; if TRUE uses future.apply.}

\item{parallel_over}{"spec" (recommended) or "fold".}

\item{workers}{Integer; number of workers. Default uses future::availableCores().}

\item{strategy}{"auto"|"multisession"|"multicore"|"sequential".}
}
\value{
List with:
\itemize{
  \item \code{by_season}: per-spec per-heldout-season metrics
  \item \code{by_spec}: aggregated metrics per spec_id
  \item \code{by_spec_grid}: by_spec joined back to the grid
  \item \code{best}: best spec row
  \item \code{scoring}: k_t and w_early used
  \item \code{timing}: elapsed and cpu seconds
}
}
\description{
For each spec_id in \code{spec_grid$specs}:
\enumerate{
  \item Prepare features once via \code{prep_stage2_m1_features()}.
  \item For each held-out season, fit \code{train_stage2_joint_m1()} on the remaining seasons.
  \item Predict on held-out season excluding season-dependent terms (new-season evaluation).
  \item Score using weighted NLL / Brier / RMSE(p).
}
}
\details{
Weighting uses **target-week time since ignition**:
\deqn{t_\text{target} = (w + h) - w^{(ign)}}
Rows with \code{0 <= t_target <= k_t} get weight \code{w_early} (default 2),
later rows weight 1.
}
