% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trainIgnition.R
\name{fitIgnition}
\alias{fitIgnition}
\title{Fit ignition-event classifiers, append p_cls columns, and summarize historical ignition stats}
\usage{
fitIgnition(
  dat,
  season_col = "season",
  week_col = "weekF",
  phase_col = "phase",
  p_col = "p",
  cum_col = "cum_p_obs",
  event_k = 1L,
  keep_after_ignition = 2L,
  k_week = 4L,
  k_p = 8L,
  k_cum = 4L,
  fit_full = TRUE,
  fit_p_only = TRUE,
  fit_cum_only = TRUE,
  verbose = TRUE,
  select = FALSE
)
}
\description{
Fits up to three small-sample-safe GAMM ignition-event classifiers and returns:
1) The input data with appended columns:
   - `p_cls`     : (weekF + cum_p + p)
   - `p_cls_p`   : (weekF + p)
   - `p_cls_cum` : (weekF + cum_p)
2) A per-season summary at the TRUE ignition week (first week with `phase==1`):
   - `iWeek_true`
   - `p_at_iWeek`, `cum_at_iWeek` (where `cum_at_iWeek` is cum_p = cumsum(p))
   - `p_up_run_before_iWeek`: number of consecutive week-to-week increases in `p`
     immediately leading into iWeek (bounded by available history).
}
\details{
Required columns (defaults): `season`, `weekF`, `phase`, `p`, `cum_p_obs`.
Note: `cum_p_obs` is no longer used for modeling; we compute `cum_p := cumsum(p)`.
}
