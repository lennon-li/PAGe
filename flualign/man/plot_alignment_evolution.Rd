% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m1_alignment_plots.R
\name{plot_alignment_evolution}
\alias{plot_alignment_evolution}
\title{Plot multiple aligned curves (full season) as observation window grows}
\usage{
plot_alignment_evolution(
  currentSeason,
  season,
  startWeek,
  start_cut_week,
  g_ref_fun,
  g_ref_mu_se,
  hyper,
  ref_df,
  allow_scale = NULL,
  force_allow_scale_from_week = NULL,
  max_newWeek = 52,
  peak_newWeek = NULL,
  k_smooth_post = 8,
  use_weights = TRUE,
  level = 0.95
)
}
\arguments{
\item{currentSeason}{data frame with at least:
newWeek, weekF, y, neg, date (date can be NA; will be derived).}

\item{season}{character like "2017-2018" (you already use this).}

\item{startWeek}{integer, starting week-of-year for the template season.}

\item{start_cut_week}{epidemiologic week (in `currentSeason$week`) at which
you start re-fitting (e.g. 44).}

\item{g_ref_fun}{reference spline on link scale.}

\item{g_ref_mu_se}{function(u) returning list(mu, se) from GAM.}

\item{hyper}{list from learn_alignment_hyperparams().}

\item{ref_df}{data frame with reference curve, typically having
`newWeek` and `p_gamm`.}

\item{allow_scale}{base setting passed to align_forecast_pipeline_dilate()
if no override is triggered (can be TRUE/FALSE/NULL).}

\item{force_allow_scale_from_week}{epi week; from this week onward
(in surveillance-week space) we FORCE allow_scale = TRUE for that cut.
Before that, we use `allow_scale` as given (including NULL).}

\item{max_newWeek}{optional scalar (aligned newWeek). If non-NULL, do not
fit cuts whose last observed newWeek exceeds this. Each fitted cut still
forecasts out to the season end.}

\item{peak_newWeek}{optional scalar in newWeek space giving the (known)
epidemic peak location. For cuts with last observed newWeek >= peak_newWeek,
use a GAM tail (from `forecast_post_peak_gam`) for newWeek > cut, instead
of continued alignment-based extrapolation.}

\item{k_smooth_post}{basis dimension for the post-peak GAM tail.}

\item{use_weights}{logical, passed to pipeline.}

\item{level}{CI level.}
}
\value{
plotly object with all curves; each cut has its own legend entry.
}
\description{
Plot multiple aligned curves (full season) as observation window grows
}
