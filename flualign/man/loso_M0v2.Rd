% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m0_training.R
\name{loso_M0v2}
\alias{loso_M0v2}
\title{Leave-one-season-out evaluation of M0v2 ignition detection}
\usage{
loso_M0v2(
  dat,
  grid,
  season_col = "season",
  week_col = "weekF",
  phase_col = "phase",
  p_col = "p",
  score_col = "p_cls_p",
  drop_seasons = NULL,
  exSeason_tune = NULL,
  fit_args = list(fit_base = TRUE, fit_slope = FALSE, fit_fs = FALSE, event_k = 1L, lead
    = 1L, A_pre = 6L, B_post = 6L, k_week = 6L, k_p = 8L, k_fs = 4L, select = FALSE,
    verbose = FALSE),
  tune_args = list(miss_penalty = 0, lambda = 20, kappa = 0, gamma = 25, gamma_late = 0,
    iWeek = TRUE, ncores = 10L, verbose = FALSE, progress_every = 200L),
  verbose = TRUE
)
}
\arguments{
\item{dat}{Data frame with columns \code{season}, \code{weekF},
\code{phase}, \code{p}, and the score column (default \code{p_cls_p}).}

\item{grid}{Data frame of candidate gate-threshold parameter combinations
passed to \code{tuneIgnitionGrid_M0v2()} in each fold.}

\item{season_col, week_col, phase_col, p_col, score_col}{Character strings
naming the corresponding columns in \code{dat}.}

\item{drop_seasons}{Optional character vector; seasons excluded from all
folds (neither training nor evaluation).}

\item{exSeason_tune}{Optional character vector; additional seasons excluded
from tuning only (not from evaluation).}

\item{fit_args}{Named list of arguments forwarded to \code{fitIgnition()}
on each fold.}

\item{tune_args}{Named list of arguments forwarded to
\code{tuneIgnitionGrid_M0v2()} on each fold.}

\item{verbose}{Logical; print fold-level progress messages (default
\code{TRUE}).}
}
\value{
A list with per-fold outputs (\code{fold_out}) and aggregated
  summary data frames (\code{eval_all}, \code{eval_tune},
  \code{eval_excluded}).
}
\description{
Performs strict LOSO cross-validation for the M0v2 ignition model. In
each fold the held-out season is withheld from both training the GAM
classifier (\code{fitIgnition}) and tuning the gate thresholds
(\code{tuneIgnitionGrid_M0v2}). Returns per-fold outputs and aggregated
detection accuracy metrics.
}
\keyword{internal}
