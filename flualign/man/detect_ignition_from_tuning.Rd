% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m0_training.R
\name{detect_ignition_from_tuning}
\alias{detect_ignition_from_tuning}
\title{Fit ignition classifier and run season-level detection}
\usage{
detect_ignition_from_tuning(
  tuned,
  alignedD,
  score_col = "p_cls_p",
  keep_signals = TRUE,
  iWeek = TRUE,
  verbose = TRUE,
  fit_base = TRUE,
  fit_slope = FALSE,
  fit_fs = FALSE,
  event_k = 1L,
  lead = 1L,
  A_pre = 6L,
  B_post = 6L,
  k_week = 6L,
  k_p = 8L
)
}
\arguments{
\item{tuned}{List returned by a LOSO tuning step (e.g.
\code{tuneIgnitionGrid_M0v2()}). Must contain \code{$best_params} as a
named list of M0 gate thresholds.}

\item{alignedD}{Data frame of aligned historical seasons passed to
\code{fitIgnition()}.}

\item{score_col}{Character; column in \code{alignedD} used as the GAM
classifier score (default \code{"p_cls_p"}).}

\item{keep_signals}{Logical; if \code{TRUE} retains per-week signal columns
in the output (default \code{TRUE}).}

\item{iWeek}{Logical; if \code{TRUE} also estimates \code{iWeek_hat}
(default \code{TRUE}).}

\item{verbose}{Logical; controls progress messages (default \code{TRUE}).}

\item{fit_base, fit_slope, fit_fs}{Logicals passed to \code{fitIgnition()}
controlling which GAM smooth terms to include.}

\item{event_k, lead, A_pre, B_post, k_week, k_p}{Integer/numeric knobs passed
to \code{fitIgnition()}.}
}
\value{
Output of \code{detectIgnitionBySeason_M0v2()}; a list with
  \code{$data} (per-week signals), \code{$by_season} (per-season
  summaries), and optionally \code{$compare} (truth vs estimate).
}
\description{
Convenience wrapper that calls \code{fitIgnition()} with the supplied
knob arguments and then passes the fitted object to
\code{detectIgnitionBySeason_M0v2()} using the best parameters from a
previous LOSO tuning run.
}
\keyword{internal}
