% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m0_training.R
\name{tuneIgnitionGrid_M0v2}
\alias{tuneIgnitionGrid_M0v2}
\title{Grid-search tuning for M0v2 ignition detector}
\usage{
tuneIgnitionGrid_M0v2(
  ign_fit,
  grid,
  score_col = "p_cls_p",
  week_col = "weekF",
  season_col = "season",
  phase_col = "phase",
  truth_col = "iWeek",
  exSeason = NULL,
  miss_penalty = 0,
  lambda = 20,
  kappa = 0,
  gamma = 25,
  gamma_late = 0,
  iWeek = TRUE,
  ncores = 10L,
  verbose = TRUE,
  progress_every = 200L
)
}
\arguments{
\item{ign_fit}{Either [fitIgnition()] output (list with \code{$data}) or a data.frame/data.table.}

\item{grid}{data.frame of parameter combinations; missing columns are filled by defaults.}

\item{score_col}{Character. Classifier score column name. Default \code{"p_cls_p"}.}

\item{week_col, season_col}{Column names for within-season week and season.}

\item{phase_col}{Column name used if \code{truth_col} is unavailable.}

\item{truth_col}{Column name for truth ignition week if stored.}

\item{exSeason}{Optional character vector of seasons to exclude from tuning (but still evaluate afterward).}

\item{miss_penalty}{Numeric. Penalty per missing detection. Default 0 (no misses occur due
to the \code{w_max} fallback in \code{detectIgnitionBySeason_M0v2}).}

\item{lambda}{Numeric. Weight on worst-case adjusted error \code{max_abs}.}

\item{kappa}{Numeric. Extra weight on late errors. Default 0 (symmetric scoring).}

\item{gamma}{Numeric. Penalty for adjusted error exceeding 2 weeks.}

\item{gamma_late}{Numeric. Extra penalty for being late >2 weeks. Default 0 (disabled).}

\item{iWeek}{Logical. Use \code{truth_col} if available; otherwise infer from \code{phase_col}.}

\item{ncores}{Integer >= 1. Number of cores.}

\item{verbose}{Logical. Print progress.}

\item{progress_every}{Integer. Chunk size for progress updates.}
}
\value{
List with best params, full results, runtime, and evaluation tables.
}
\description{
Tunes M0v2 ignition thresholds over a parameter grid by repeatedly calling
[detectIgnitionBySeason_M0v2()] and comparing estimated ignition weeks to season-level
truth ignition weeks. Scoring uses a symmetric adjusted error with a -1 week wiggle room
(detecting one week early counts as exact). Seasons with no detection within
\code{[w_min, w_max]} are assigned \code{w_max} as a fallback, so misses never occur.
}
