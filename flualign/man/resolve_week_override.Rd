% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prospective_running.R
\name{resolve_week_override}
\alias{resolve_week_override}
\title{Resolve a week estimate with an optional manual override}
\usage{
resolve_week_override(
  week_est,
  override_week = NULL,
  mode = c("replace", "cap", "floor", "nearest_valid"),
  valid_weeks = 1:52
)
}
\arguments{
\item{week_est}{Integer-ish scalar estimate (can be \code{NA}).}

\item{override_week}{Optional integer-ish scalar override (can be \code{NULL}/\code{NA}).}

\item{mode}{Override policy.}

\item{valid_weeks}{Integer vector of valid week values. Default 1:52.}
}
\value{
A list with elements \code{final}, \code{est}, \code{overridden}, \code{override}.
}
\description{
Applies an override week to an estimated week using a selected policy:
\itemize{
\item \code{"replace"}: force the week to the override.
\item \code{"cap"}: final week cannot be later than override (\code{min(est, override)}).
\item \code{"floor"}: final week cannot be earlier than override (\code{max(est, override)}).
\item \code{"nearest_valid"}: snap override to the nearest value in \code{valid_weeks}.
}
}
\examples{
resolve_week_override(18, NULL)
resolve_week_override(18, 20, mode = "cap")
resolve_week_override(NA, 15, mode = "replace")
}
