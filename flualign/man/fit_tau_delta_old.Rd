% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m1_fit.R
\name{fit_tau_delta_old}
\alias{fit_tau_delta_old}
\title{Fit tau and delta alignment parameters (legacy version)}
\usage{
fit_tau_delta_old(
  currentD,
  g_ref_fun,
  tau_bounds,
  delta_bounds,
  allow_scale = NULL,
  week_threshold_delta,
  lam_delta,
  use_weights = TRUE
)
}
\arguments{
\item{currentD}{Data frame with columns \code{newWeek}, \code{y}, and
\code{neg} for one season.}

\item{g_ref_fun}{Reference curve function (unrestricted domain).}

\item{tau_bounds}{Numeric vector of length 2; box constraint on \code{tau}.}

\item{delta_bounds}{Numeric vector of length 2; box constraint on
\code{delta}.}

\item{allow_scale}{Logical or \code{NULL}. If \code{NULL} (default),
scale is enabled once \code{max(newWeek) >= 28}.}

\item{week_threshold_delta}{Integer; \code{delta} is freed only when
\code{max(newWeek) >= week_threshold_delta}.}

\item{lam_delta}{Numeric; ridge penalty coefficient on \code{delta}.}

\item{use_weights}{Logical; if \code{TRUE} (default) weights each
observation by \code{n = y + neg}.}
}
\value{
A list with \code{tau}, \code{a}, \code{b}, \code{delta},
  \code{allow_scale}, \code{delta_on}, \code{value}, \code{status}, and
  \code{predict_prob}.
}
\description{
Older implementation of the joint \eqn{(\tau, \delta)} fitting step.
Uses \code{nloptr::sbplx()} to minimise the penalised binomial NLL
\eqn{-\sum w \, \ell(\tau, a, b, \delta) / n + \lambda \delta^2}.
Replaced by the multi-template ensemble approach; retained for reference.
}
\keyword{internal}
