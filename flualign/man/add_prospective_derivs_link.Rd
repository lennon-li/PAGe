% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_prospective_derivs_link.R
\name{add_prospective_derivs_link}
\alias{add_prospective_derivs_link}
\title{Prospective (real-time safe) derivatives of positivity on the logit scale}
\usage{
add_prospective_derivs_link(alignedD, k = 5L, eps = 1e-06, min_obs = 4L)
}
\arguments{
\item{alignedD}{A data frame containing (at minimum) columns \code{season}, \code{weekF}, \code{y}, \code{neg}.
Here \code{y} is positive count and \code{neg} is negative count for each season-week.}

\item{k}{Integer window size (number of most recent weeks up to the current week) used for the local quadratic.
Defaults to \code{5L}. Larger values smooth more but may lag.}

\item{eps}{Small positive constant used to clamp positivity away from 0 and 1 before applying \code{qlogis}.
Defaults to \code{1e-6}.}

\item{min_obs}{Minimum number of observations required in the local window to fit the quadratic.
Defaults to \code{4L}. If fewer are available, derivatives are returned as \code{NA}.}
}
\value{
A tibble/data.frame with the original columns plus:
\describe{
  \item{\code{y_w}}{Observed positivity \eqn{y_{s,w}} on probability scale}
  \item{\code{z_w}}{Logit-transformed positivity \eqn{z_{s,w}}}
  \item{\code{d1_link}}{Prospective first derivative on the logit scale}
  \item{\code{d2_link}}{Prospective second derivative on the logit scale}
}
}
\description{
Computes prospective first and second derivatives of weekly test positivity
using only information available up to each week (no future leakage).
}
\details{
For each season and each week \eqn{w}, the function:
\itemize{
  \item computes \eqn{y_{s,w} = x_{s,w} / n_{s,w}} from columns \code{y} and \code{neg}
  \item transforms to link scale \eqn{z_{s,w} = \mathrm{logit}(y_{s,w})} with clamping
  \item fits a local quadratic on the last \code{k} points up to \eqn{w}:
        \eqn{z \approx a + b u + c u^2} where \eqn{u = w' - w} is centered at \eqn{w}
  \item returns \eqn{d1 = b} and \eqn{d2 = 2c} as prospective derivatives at \eqn{w}
}

This local, one-sided (history-only) estimator is stable at the boundary and
suitable for prospective ignition detection and real-time pipelines.


The derivatives are computed per season and per week using only the prefix of the season up to that week.
This avoids optimistic performance that can arise if derivatives are computed from smooths fit to full seasons.
}
\examples{
\dontrun{
alignedD_prosp <- add_prospective_derivs_link(alignedD, k = 5L)
head(alignedD_prosp[, c("season","weekF","y_w","d1_link","d2_link")])
}

}
