% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2_nested_loso.R
\name{add_prospective_derivs_link}
\alias{add_prospective_derivs_link}
\title{Prospective (real-time safe) derivatives of positivity on the logit scale}
\usage{
add_prospective_derivs_link(alignedD, k = 5L, eps = 1e-06, min_obs = 4L)
}
\arguments{
\item{alignedD}{Data frame with columns \code{season}, \code{weekF},
\code{y}, and \code{neg}.}

\item{k}{Integer; window size for local quadratic fit (default 5L).}

\item{eps}{Numeric; clipping epsilon for logit (default 1e-6).}

\item{min_obs}{Integer; minimum observations required (default 4L).}
}
\value{
\code{alignedD} with additional columns \code{d1_link} and
  \code{d2_link}.
}
\description{
For each season in \code{alignedD}, fits a local quadratic to a rolling
window of \code{k} observations on the logit scale and returns the
instantaneous first and second derivatives as \code{d1_link} and
\code{d2_link}. Computation is strictly causal: only observations up to
and including the current week are used.
}
