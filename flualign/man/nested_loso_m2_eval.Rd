% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2_nested_loso.R
\name{nested_loso_m2_eval}
\alias{nested_loso_m2_eval}
\title{Evaluate M2 on the held-out test season}
\usage{
nested_loso_m2_eval(
  allD,
  fold,
  m2_fit,
  m1_test_preds,
  spec,
  eval_window = 12L,
  k_deriv = 10L,
  manual_labels = NULL,
  flag_args = list(p_thresh = 0.01, k1 = 0.4, k_c = 0.01, n_consec = 2L, min_window =
    10L, w_min = 21L, w_max = 21L, d2_relax = -0.01),
  verbose = TRUE
)
}
\arguments{
\item{allD}{Full multi-season data frame.}

\item{fold}{Output of \code{nested_loso_build_fold()}.}

\item{m2_fit}{Output of \code{nested_loso_m2_train()} (the trained M2 GAM).}

\item{m1_test_preds}{M1 test predictions from \code{nested_loso_m1_test()}.}

\item{spec}{M2 hyperparameter spec object.}

\item{eval_window}{Integer; maximum weeks post-ignition to evaluate
(default 12L).}

\item{k_deriv}{Integer; basis dimension for \code{estimateDerivs()}.}

\item{manual_labels}{Optional manual ignition labels.}

\item{flag_args}{Named list forwarded to \code{flagIgnition()}.}

\item{verbose}{Logical; print progress.}
}
\value{
A named list:
  \describe{
    \item{scores}{One-row tibble with season, n, mean_nll, brier, rmse_p.}
    \item{predictions}{Tibble with per-observation predictions
      (season, weekF, lead, t_since, p_hat, p_obs, y_lead, N_lead).}
  }
  On failure, scores contain \code{NA} and predictions is empty.
}
\description{
Builds aligned test data (running M0 pipeline on the test season),
prepares M2 features with the fold's per-fold \code{template_df},
scores predictions, and extracts diagnostics.
}
