% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detectIgnitionBySeason.R
\name{detectIgnition_oneSeason}
\alias{detectIgnition_oneSeason}
\title{Core ignition detector for ONE season (prospective / real-time safe)}
\usage{
detectIgnition_oneSeason(df_one, params)
}
\arguments{
\item{df_one}{One-season data (can be partial season, prospective safe)}

\item{params}{Threshold list: cls_thr, p_cum_thr, p_thr, prev_thr, n_consec, d1_thr, N, w_min, w_max}
}
\value{
list(ignite, iWeek_hat, now, signals)
}
\description{
Required columns: weekF, p, p_cls_p, y, N
Derived:
  cum_p := cumsum(p)
  prev  := cumsum(y) / cumsum(N)    (old cum positivity)
}
\details{
Signals (6):
1) p_cls_p >= cls_thr
2) cum_p   >= p_cum_thr            (cum_p = cumsum(p))
3) p       >= p_thr
4) prev    >= prev_thr             (prev = cumsum(y)/cumsum(N))
5) strict increases for n_consec weeks (last n_consec-1 diffs > 0)
6) min d1 over last n_consec-1 diffs >= d1_thr

Ignition eligible if: (w_min <= weekF <= w_max) & (n_hit >= N)
}
