% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m1_m2_bridge.R
\name{run_m0_m1_m2_weekly}
\alias{run_m0_m1_m2_weekly}
\title{Run full M0→M1→M2 weekly forecast chain}
\usage{
run_m0_m1_m2_weekly(
  currentSeason,
  ref,
  hyper,
  stage2_fit,
  kit = NULL,
  params = NULL,
  ign_out = NULL,
  allow_scale = NULL,
  level = 0.95,
  use_m1_template = TRUE,
  exclude = NULL,
  exclude_season_re = TRUE,
  interval = c("pi", "ci")
)
}
\arguments{
\item{currentSeason}{Data frame for the current season up to this week.}

\item{ref}{Reference object from \code{estimateRef()}.}

\item{hyper}{M1 hyperparams from \code{learn_alignment_hyperparams()}.}

\item{stage2_fit}{Fitted M2 model (bam/gam object).}

\item{kit}{Prospective kit (used by \code{build_stage2_pseudo_prospective_list()}).}

\item{params}{M0 detection params (if \code{ign_out} is NULL).}

\item{ign_out}{Pre-computed M0 ignition output.}

\item{allow_scale}{Passed to M1 alignment.}

\item{level}{Confidence level (default 0.95).}

\item{use_m1_template}{Logical. If TRUE (default), replaces M2's template
with M1's aligned prediction. If FALSE, uses static template (legacy mode).}

\item{exclude}{M2 prediction exclude terms (e.g., for new season).}

\item{exclude_season_re}{Logical (default TRUE).}

\item{interval}{"pi" or "ci" for M2 prediction intervals.}
}
\value{
A list with:
\describe{
  \item{m1}{Full M1 alignment result from \code{run_alignment_prospective()}}
  \item{m2_forecast}{M2 forecast data frame from \code{stage2_predict_series()}}
  \item{state}{Overall pipeline state: "pre_ignition", "aligning", or "post_peak"}
}
}
\description{
Convenience function for prospective deployment that chains:
\enumerate{
  \item M0 ignition detection (via \code{ign_out} or \code{params})
  \item M1 alignment (via \code{run_alignment_prospective()})
  \item M2 forecast with M1's prediction as template
}
}
