% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ignitionTraining.R
\name{detectIgnitionBySeason_M0v2}
\alias{detectIgnitionBySeason_M0v2}
\title{Prospective ignition detection (M0v2) across seasons}
\usage{
detectIgnitionBySeason_M0v2(
  ign_fit,
  params,
  score_col = "p_cls_p",
  season_col = "season",
  week_col = "weekF",
  y_col = "y",
  N_col = "N",
  phase_col = "phase",
  truth_col = "iWeek",
  keep_signals = TRUE,
  verbose = TRUE,
  iWeek = FALSE,
  copy_data = TRUE
)
}
\arguments{
\item{ign_fit}{Either a list returned by [fitIgnition()] containing \code{$data}, or a data.frame/data.table.}

\item{params}{Named list of thresholds/hyperparameters.}

\item{score_col}{Character. Name of classifier score column. Default \code{"p_cls_p"}.}

\item{season_col, week_col}{Column names for season and within-season week.}

\item{y_col, N_col}{Column names for positives and totals.}

\item{phase_col}{Column name for phase indicator (used for truth if \code{truth_col} missing).}

\item{truth_col}{Column name for truth ignition week if stored explicitly.}

\item{keep_signals}{Logical. If TRUE return full row-level signals.}

\item{verbose}{Logical. If TRUE prints summary.}

\item{iWeek}{Logical. If TRUE return season-level compare table.}

\item{copy_data}{Logical. If FALSE operate on input data.table by reference.}
}
\value{
list with \code{by_season} and optionally \code{data} and \code{compare}.
}
\description{
Applies a prospective-safe ignition detector across all seasons. The detector uses five gates:
\enumerate{
  \item classifier score gate: \code{score_col >= cls_thr}
  \item rolling-sum evidence gate: \code{p_sumK >= p_sum_thr} where \code{p_sumK = rollsum(p, K_sum)}
  \item smoothed positivity level gate: \code{p_sm >= p_thr} where \code{p_sm = rollmean(p, L)}
  \item cumulative prevalence gate: \code{prev >= prev_thr} where \code{prev = cumsum(y)/cumsum(N)}
  \item noise-tolerant trend gate on \code{p_sm} requiring sustained increases with tolerance \code{eps}
}
}
\details{
Within the eligible window \code{w_min <= week <= w_max}, ignition is declared at the earliest
week where at least \code{N_req} of the five gates are satisfied (N-of-5 voting). The classifier
gate is a vote (not mandatory).
}
