% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retired.R
\name{check_scale_identifiability_old}
\alias{check_scale_identifiability_old}
\title{Leave-one-season-out evaluation of the full M0→M1→M2 pipeline}
\usage{
check_scale_identifiability_old(
  currentD,
  g_ref_fun,
  hyper,
  min_week = 20,
  min_range_p = 0.1,
  min_range_g = 0.5
)
}
\arguments{
\item{allD}{Multi-season data frame with columns weekF, y, neg, season.}

\item{params}{M0 detection parameters.}

\item{spec}{M2 spec from \code{stage2_make_spec()}.}

\item{template_df}{Template curve (newWeek, fit). Used as fallback when
M1 predictions are unavailable.}

\item{manual_labels}{Named list of manual ignition labels per season.}

\item{test_seasons}{Character vector of seasons to hold out. If NULL,
uses all seasons.}

\item{exclude_seasons}{Seasons to exclude entirely.}

\item{horizons}{Forecast horizons (default \code{c(1L, 2L)}).}

\item{eval_window}{Integer; post-ignition weeks to evaluate (default 12L).}

\item{k_deriv}{Integer; derivative fitting window (default 10L).}

\item{k_ref}{Integer; reference GAM basis size (default 10L).}

\item{n_weeks}{Integer; number of weeks in a season (default 52L).}

\item{flag_args}{List of ignition detection parameters.}

\item{allow_scale}{Passed to alignment.}

\item{use_ci}{Passed to alignment.}

\item{buffer_weeks}{Passed to alignment.}

\item{min_obs}{Minimum observations for alignment.}

\item{curvature_ratio}{Passed to alignment.}

\item{method}{BAM fitting method (default "REML").}

\item{n_cores}{Number of parallel workers.}

\item{verbose}{Logical.}
}
\value{
A list with:
\describe{
  \item{scores}{Tibble with per-season NLL, Brier, RMSE scores}
  \item{predictions}{Tibble with all M2 predictions vs actuals}
  \item{m1_preds_list}{List of M1 predictions per fold (for diagnostics)}
}
}
\description{
For each test season, this function:
\enumerate{
  \item Builds aligned training data and fits reference curve (M1 training)
  \item Runs M1 walk-forward for each training season to generate M1
    predictions (template features for M2)
  \item Trains M2 with M1 predictions as the template feature (stacking)
  \item Runs M1 walk-forward on the test season and evaluates M2
}
}
