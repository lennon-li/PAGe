% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m1_peak_flags.R
\name{flagPeak}
\alias{flagPeak}
\title{Flag whether the epidemic has passed its peak (real time)}
\usage{
flagPeak(
  df,
  value_col = NULL,
  min_week = 1L,
  max_week = Inf,
  drop_frac = 0.25,
  min_abs_drop = 0.05,
  min_consec_below = 2L
)
}
\arguments{
\item{df}{data frame for a single season, with at least:
\itemize{
  \item newWeek: monotone-increasing epidemic week index
  \item p: positivity (if missing, y/(y+neg) will be computed)
}}

\item{value_col}{name of the column to treat as positivity (default: "p";
if NULL and p is absent, uses y/(y+neg)).}

\item{min_week}{minimum newWeek to start considering a "past-peak" flag.}

\item{max_week}{maximum newWeek to consider (Inf = no upper limit).}

\item{drop_frac}{required relative drop from the peak_so_far:
flag when rel_drop >= drop_frac.}

\item{min_abs_drop}{required absolute drop from peak_so_far:
flag when abs_drop >= min_abs_drop.}

\item{min_consec_below}{number of consecutive weeks that must satisfy
the drop condition before declaring "past peak".}
}
\value{
A list with:
  \itemize{
    \item df: original data plus helper columns:
      \code{peak_so_far}, \code{abs_drop}, \code{rel_drop},
      \code{drop_cond}, \code{drop_streak}, \code{eligible},
      \code{past_peak_flag}
    \item flag_week: newWeek at which "past peak" is first flagged (NA if none)
    \item peak_week_so_far: newWeek of the global max within df
    \item peak_value_so_far: value at that peak
  }
}
\description{
Uses only information up to each week (dynamic peak_so_far), and
checks whether there has been a sustained drop from that peak.
}
