% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimateRef.R
\name{estimateDerivs}
\alias{estimateDerivs}
\title{Estimate smoothed positivity and derivatives (d1/d2) by season using binomial GAMs}
\usage{
estimateDerivs(
  allD,
  k = 10,
  bs = "ps",
  week_col = "weekF",
  season_col = "season",
  y_col = "y",
  n_col = "N",
  ci_level = 0.95,
  deriv_interval = "simultaneous",
  method = "REML",
  peak_weight_boost = 1,
  peak_weight_decay = 0.3,
  ignition_weeks = NULL
)
}
\arguments{
\item{allD}{A data.frame containing (at minimum) season, week index, positives, and tests.}

\item{k}{Integer. Basis dimension passed to \code{mgcv::s()} for the within-season smooth.}

\item{bs}{Character. Smoother basis for \code{mgcv::s()}, e.g. \code{"ps"} or \code{"tp"}.}

\item{week_col}{Character. Column name for the within-season week index used in smoothing
(default \code{"weekF"}).}

\item{season_col}{Character. Column name identifying season (default \code{"season"}).}

\item{y_col}{Character. Column name for positives (default \code{"y"}).}

\item{n_col}{Character. Column name for tests (default \code{"N"}).}

\item{ci_level}{Numeric in (0,1). Confidence level for fitted mean intervals on the response
scale (default 0.95).}

\item{deriv_interval}{Character. Interval type passed to \code{gratia::derivatives()},
typically \code{"simultaneous"} (default) or \code{"confidence"}.}

\item{method}{Character. Smoothing parameter estimation method passed to \code{mgcv::gam()},
default \code{"REML"}.}

\item{peak_weight_boost}{Numeric >= 1. Multiplicative weight applied to observations
between ignition and observed peak (default 1 = no boost).}

\item{peak_weight_decay}{Numeric > 0. Exponential decay rate for weights after the
observed peak. Smaller values = slower decay (default 0.3, ~2-week half-life).}

\item{ignition_weeks}{Optional named integer vector mapping season labels to ignition
week values (in \code{week_col} space). Required when \code{peak_weight_boost > 1}.
If \code{NULL} and boosting is requested, falls back to the first week where
\code{p = y/N >= 0.01}.}
}
\value{
A list with two elements:
\describe{
  \item{data}{A data.frame of \code{allD} augmented with columns:
    \code{neg}, \code{fit}, \code{fit_low}, \code{fit_high},
    \code{d1}, \code{d1_low}, \code{d1_high},
    \code{d2}, \code{d2_low}, \code{d2_high}.}
  \item{models}{A named list of fitted \code{mgcv::gam} objects (one per season).}
}
}
\description{
Fits a separate binomial GAM for each season to smooth weekly positivity and
computes first and second derivatives of the fitted smooth. Returns the input
data augmented with fitted values, confidence intervals, and derivative estimates
(with simultaneous intervals), plus the fitted model objects.
}
\details{
When \code{peak_weight_boost > 1}, observations between ignition and the observed
peak receive higher weight, with a soft exponential decay after the peak. This
emphasises the rising-limb-through-peak region that downstream alignment and
forecasting (M2) care about most.
}
