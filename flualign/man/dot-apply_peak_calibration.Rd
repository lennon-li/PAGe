% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m1_runtime.R
\name{.apply_peak_calibration}
\alias{.apply_peak_calibration}
\title{Apply Bayesian shrinkage + bias correction to a peak estimate}
\usage{
.apply_peak_calibration(
  t_peak,
  t_peak_lo,
  t_peak_hi,
  t_since_ign,
  cal,
  level = 0.95
)
}
\arguments{
\item{t_peak}{Numeric. Raw peak estimate in \code{newWeek} space.}

\item{t_peak_lo}{Numeric. Lower CI bound in \code{newWeek} space.}

\item{t_peak_hi}{Numeric. Upper CI bound in \code{newWeek} space.}

\item{t_since_ign}{Numeric. Weeks since true ignition (\code{eval_week - iWeek_hat}).}

\item{cal}{List. Output of \code{fit_peak_calibration()}.}

\item{level}{Numeric. CI level (default \code{0.95}).}
}
\value{
Named list with \code{t_peak}, \code{t_peak_lo}, \code{t_peak_hi}.
}
\description{
Internal helper used by \code{run_alignment_prospective()} when
\code{cal} is provided. Applies two corrections sequentially:
\enumerate{
  \item \strong{Shrinkage (C):} pulls \code{t_peak} toward the historical
    prior mean, weighted by the ratio of prior variance to data variance
    (CI width). Early-season wide CIs → heavy shrinkage.
  \item \strong{Bias correction (A):} subtracts the residual bias predicted
    by a GAM fitted on LOSO errors as a function of \code{t_since_ign}.
}
Returns calibrated \code{t_peak} and updated CI bounds (posterior variance).
}
\keyword{internal}
