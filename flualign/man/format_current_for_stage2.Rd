% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2_training.R
\name{format_current_for_stage2}
\alias{format_current_for_stage2}
\title{Format current-season observations for Stage-2 refit}
\usage{
format_current_for_stage2(
  currentSeason,
  iWeek_used,
  template_df = NULL,
  spec = NULL,
  season_label = "current"
)
}
\arguments{
\item{currentSeason}{data.frame with columns \code{weekF}, \code{y}, and either
\code{N} (total tests) or \code{neg} (negative tests).}

\item{iWeek_used}{Integer. Ignition week on the \code{weekF} scale.}

\item{template_df}{data.frame with columns \code{newWeek} and \code{fit} (unused
here but retained for signature compatibility with \code{refit_stage2_weekly}).}

\item{spec}{Stage-2 spec from \code{stage2_make_spec()}. Must contain
\code{spec$anchorWeek}.}

\item{season_label}{Character label for the current season (default \code{"current"}).}
}
\value{
data.frame with the required Stage-2 columns, ready to \code{rbind} with
  historical aligned data.
}
\description{
Converts raw current-season surveillance data and M1 alignment outputs into the
column set required by \code{train_stage2_joint()}: \code{season}, \code{weekF},
\code{phase}, \code{newWeek}, \code{y}, \code{N}, \code{neg}.
}
