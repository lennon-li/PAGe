% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2_training.R
\name{refit_stage2_weekly}
\alias{refit_stage2_weekly}
\title{Refit Stage-2 GAM with current-season data for weekly prospective forecasting}
\usage{
refit_stage2_weekly(
  current_obs,
  iWeek_used,
  hist_data,
  template_df,
  spec,
  m1_preds = NULL,
  season_label = "current",
  addFS = NULL,
  verbose = TRUE
)
}
\arguments{
\item{current_obs}{data.frame with columns \code{weekF}, \code{y}, \code{N}
(or \code{y}/\code{neg}) for the current season up to the current week.}

\item{iWeek_used}{Numeric. Detected ignition week (on weekF scale).}

\item{hist_data}{\code{alignedD_prosp} — historical aligned dataset (all past seasons).}

\item{template_df}{Template curve with \code{newWeek} and \code{fit}.}

\item{spec}{Stage-2 spec from \code{stage2_spec_from_tuning()}.}

\item{season_label}{Character. Label for the current season (default \code{"current"}).}

\item{addFS}{Integer threshold for re-enabling the season-specific factor-smooth
in a brand-new season refit. If \code{NULL} (default), the factor-smooth is
never included in weekly refits for unseen seasons. If an integer is given,
the original \code{spec$k_s} is restored once at least that many post-ignition
origin weeks are available in \code{current_obs}.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
Output of \code{train_stage2_joint()} on the combined dataset.
}
\description{
Combines all historical aligned data with the current season's observations
(formatted via \code{format_current_for_stage2}) and refits the Stage-2 GAM.
Call this function each week after ignition detection to obtain an updated model
that has estimated the live season's random effect and factor-smooth.
}
