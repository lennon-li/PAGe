% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_training.R
\name{m1_make_params}
\alias{m1_make_params}
\title{Construct an M1 alignment parameter list}
\usage{
m1_make_params(
  k_ref = 25L,
  ref_method = "fs",
  temperature = 0.25,
  rise_weight = 1,
  trough_weight = 0.1,
  peak_decay = 0.3,
  slope_weight = 8,
  slope_window = 6L,
  dynamic_temp = FALSE,
  dynamic_temp_pivot = 10L
)
}
\arguments{
\item{k_ref}{Integer. Reference curve GAM basis dimension (default 25).}

\item{ref_method}{Character. Reference fitting method passed to
\code{estimateRef()}. One of \code{"fs"} (factor-smooth, default) or
\code{"re"}.}

\item{temperature}{Numeric. Softmax temperature for template weighting
(default 0.25). Lower values concentrate weight on the best-matching
template.}

\item{rise_weight}{Numeric. Weight given to rise-phase similarity (default
1.0).}

\item{trough_weight}{Numeric. Weight given to trough similarity (default
0.1).}

\item{peak_decay}{Numeric. Exponential decay on peak-proximity weight
(default 0.3).}

\item{slope_weight}{Numeric. Weight on slope similarity at aligned positions
(default 8.0).}

\item{slope_window}{Integer. Number of weeks used to compute the local slope
(default 6).}

\item{dynamic_temp}{Logical. If \code{TRUE}, temperature adapts over the
season (default \code{FALSE}).}

\item{dynamic_temp_pivot}{Integer. Week at which dynamic temperature pivots
(default 10; ignored when \code{dynamic_temp = FALSE}).}
}
\value{
A named list suitable for the \code{m1_params} argument of
  \code{build_m1()}, \code{build_m2()}, and \code{train_m2()}.
}
\description{
Builds the named list consumed by \code{build_m1()}, \code{build_m2()}, and
\code{train_m2()} via their \code{m1_params} argument. Calling this function
is the recommended way to customise alignment settings rather than
hand-crafting a raw list, as it documents every knob and enforces defaults.
}
\examples{
params <- m1_make_params()
params_custom <- m1_make_params(slope_weight = 12, temperature = 0.15)

}
