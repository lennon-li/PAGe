% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2_loso_eval.R
\name{nested_loso_m2_eval_weekly_refit}
\alias{nested_loso_m2_eval_weekly_refit}
\title{Evaluate a Stage-2 spec using weekly GAM refit on the test season}
\usage{
nested_loso_m2_eval_weekly_refit(
  allD,
  fold,
  m1_test_preds,
  spec,
  m1_train_preds = NULL,
  eval_window = 12L,
  horizons = c(1L, 2L),
  manual_labels = NULL,
  flag_args = list(p_thresh = 0.01, k1 = 0.4, k_c = 0.01, n_consec = 2L, min_window =
    10L, w_min = 21L, w_max = 21L, d2_relax = -0.01),
  verbose = TRUE
)
}
\arguments{
\item{allD}{Full multi-season data frame.}

\item{fold}{Fold object from \code{nested_loso_build_fold()}.}

\item{m1_test_preds}{Held-out-season M1 predictions.}

\item{spec}{Stage-2 specification.}

\item{m1_train_preds}{Optional training-season M1 predictions.}

\item{eval_window}{Maximum post-ignition evaluation week.}

\item{horizons}{Forecast horizons.}

\item{manual_labels}{Optional named ignition labels.}

\item{flag_args}{Named list forwarded to \code{flagIgnition()}.}

\item{verbose}{Logical.}
}
\value{
Same structure as \code{nested_loso_m2_eval()}: list with
  \code{scores} and \code{predictions}.
}
\description{
Variant of \code{nested_loso_m2_eval()} that uses \code{refit_stage2_weekly()}
instead of predicting from a frozen fit. For each post-ignition evaluation
week on the held-out test season, the GAM is refitted on all training-season
aligned data plus the current-season observations accumulated to that week.
}
\details{
This matches the deployment semantics of \code{run_m2_forecast(mode="weekly_refit")}.
}
