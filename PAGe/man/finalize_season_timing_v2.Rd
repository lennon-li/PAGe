% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timing_workflow_v2.R
\name{finalize_season_timing_v2}
\alias{finalize_season_timing_v2}
\title{Record user supplied ignition and peak timing labels}
\usage{
finalize_season_timing_v2(
  review,
  ignition,
  peak,
  n_weeks = NULL,
  calendar = NULL,
  annotator = NULL,
  note = NULL
)
}
\arguments{
\item{review}{An object returned by \code{review_season_timing_v2()}.}

\item{ignition}{One ignition week or two consecutive ignition weeks.}

\item{peak}{One peak week or two consecutive peak weeks.}

\item{n_weeks}{Optional number of weeks in the season. When omitted, the
larger of 52 and the maximum observed \code{weekF} is used, or the
calendar's count is used when \code{calendar} is supplied.}

\item{calendar}{Optional \code{page_timing_calendar_v2} object.}

\item{annotator}{Optional person or process identifier.}

\item{note}{Optional free-text rationale for the labels.}
}
\value{
An object of class \code{page_season_timing_v2} containing normalized
  labels, scoring references, evidence rows, and review provenance.
}
\description{
Each event accepts one week or two consecutive weeks. A singleton is
expanded to the preceding pair. Ignition is scored at the pair midpoint.
For peak, the observed week is selected from the supplied pair by the
larger observed positivity (ties select the earlier week); the other week
remains second-label provenance.
}
