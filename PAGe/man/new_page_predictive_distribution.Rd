% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictive_distribution.R
\name{new_page_predictive_distribution}
\alias{new_page_predictive_distribution}
\title{Construct a sample-based PAGe predictive distribution}
\usage{
new_page_predictive_distribution(
  draws,
  outcome,
  scale = "proportion",
  support = c(0, 1),
  origin = NULL,
  target = NULL,
  horizon = NULL,
  calibration = NULL,
  provenance = NULL,
  status = c("available", "experimental", "unavailable"),
  weights = NULL
)
}
\arguments{
\item{draws}{Numeric predictive draws. An empty vector is allowed only when
\code{status = "unavailable"}.}

\item{outcome}{One-word or short label describing the forecasted outcome.}

\item{scale}{Scale of the draws, for example \code{"proportion"}.}

\item{support}{Numeric length-two lower and upper bounds.}

\item{origin}{Optional origin metadata, typically a list with season and
week.}

\item{target}{Optional target metadata, typically a list with season and
week.}

\item{horizon}{Optional positive integer forecast horizon.}

\item{calibration}{Optional calibration metadata.}

\item{provenance}{Optional identity metadata for the forecasting and
calibration artifacts.}

\item{status}{Either \code{"available"}, \code{"experimental"}, or
\code{"unavailable"}.}

\item{weights}{Optional finite, non-negative atom weights matching
\code{draws}; positive weights are normalized to sum to one. Weighted
summaries are exact and have no Monte Carlo error interval.}
}
\value{
An object of class \code{page_predictive_distribution}.
}
\description{
This constructor is the model-agnostic entry point for engines that already
provide predictive draws. Values must be on the declared outcome scale.
}
\examples{
d <- new_page_predictive_distribution(
  c(0.01, 0.02, 0.03),
  outcome = "positivity_observed"
)
probability_above(d, 0.02)
}
\seealso{
\code{\link[=predictive_distribution]{predictive_distribution()}},
  \code{\link[=fit_forecast_calibrator]{fit_forecast_calibrator()}},
  \code{\link[=probability_above]{probability_above()}},
  \code{\link[=probability_below]{probability_below()}}

Other predictive distributions:
\code{\link[=predictive_distribution]{predictive_distribution()}}
}
\concept{predictive distributions}
