% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m1_multi_template.R
\name{align_multi_template}
\alias{align_multi_template}
\title{Multi-template ensemble alignment and forecast}
\usage{
align_multi_template(
  currentD,
  eta_mat,
  g_ref_fun,
  g_ref_mu_se,
  hyper,
  allow_scale = NULL,
  use_weights = TRUE,
  level = 0.95,
  future_weeks = NULL,
  include_observed = TRUE,
  fallback_when_unstable = TRUE,
  curvature_ratio = 1,
  temperature = 1,
  top_k = NULL,
  blend_alpha = 1,
  time_weights = NULL,
  trough_weight = 0.1,
  rise_weight = 1,
  peak_decay = 0.3,
  slope_weight = 1,
  slope_window = 6L,
  dynamic_temp = TRUE,
  dynamic_temp_pivot = 10L,
  gam_obj = NULL
)
}
\arguments{
\item{currentD}{tibble/data.frame with \code{newWeek}, \code{y}, \code{neg}.}

\item{eta_mat}{Numeric matrix (\code{n_weeks} x \code{n_seasons}) of
logit-scale per-season predictions from \code{estimateRef(method = "fs")}.}

\item{g_ref_fun}{Population reference curve function (logit scale). Used as
the blending anchor when \code{blend_alpha < 1}.}

\item{g_ref_mu_se}{Population reference uncertainty function (fallback).}

\item{hyper}{List of alignment hyperparameters (TAU_BOUNDS, etc.).}

\item{allow_scale}{Logical or NULL. Passed through.}

\item{use_weights}{Logical; use \code{y + neg} as binomial weights.}

\item{level}{CI level (default 0.95).}

\item{future_weeks}{Numeric vector of future newWeek values.}

\item{include_observed}{Logical; include observed block in pred_df.}

\item{fallback_when_unstable}{Logical; refit with delta=0 if unstable.}

\item{curvature_ratio}{Numeric; delta gate coefficient.}

\item{temperature}{Numeric; softmax temperature for NLL weighting
(default 1.0). Lower = more peaked (winner-take-all).}

\item{top_k}{Integer or NULL. If not NULL, pre-filter to top-K templates
ranked by Spearman correlation with observed data.}

\item{blend_alpha}{Numeric 0--1. Blend each per-season template toward the
population reference: \code{g_blend = (1-alpha)*g_ref + alpha*g_s}.
Default 1.0 (pure per-season curve).}

\item{time_weights}{Numeric vector or NULL; pre-computed time weights.}

\item{trough_weight}{Numeric; alignment loss trough weight.}

\item{rise_weight}{Numeric; alignment loss rise weight.}

\item{peak_decay}{Numeric; exponential decay after peak.}

\item{slope_weight}{Numeric; strength of growth-rate-aware template weighting
(default 0.5). Higher values make templates with similar recent slope to
observed data receive more weight. Set to 0 to disable.}

\item{slope_window}{Integer; number of recent weeks to compute slope over
(default 4).}

\item{dynamic_temp}{Logical; if TRUE (default), scale temperature up when
few observations available (wider ensemble early, sharper late).}

\item{dynamic_temp_pivot}{Integer; observation count below which temperature
is inflated (default 10). Temperature is scaled by
\code{pivot / n_obs} when \code{n_obs < pivot}.}

\item{gam_obj}{Optional fitted GAM used to estimate per-template uncertainty.}
}
\value{
List with the same structure as
  \code{align_forecast_pipeline_dilate()} output, plus:
  \describe{
    \item{per_template}{List of per-template alignment results.}
    \item{weights}{Named numeric vector of softmax ensemble weights.}
    \item{template_names}{Character vector of template season labels.}
  }
}
\description{
Aligns observed data to each training season's curve (from \code{eta_mat}
returned by \code{estimateRef(method = "fs")}), then ensembles the forecasts
weighted by alignment NLL via softmax.
}
