% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m1_multi_template.R
\name{.compute_logit_spread}
\alias{.compute_logit_spread}
\title{Compute weighted logit-scale spread (internal helper)}
\usage{
.compute_logit_spread(logit_mat, wts, se_mat = NULL, spread_method = "between")
}
\arguments{
\item{logit_mat}{Numeric matrix (n_future x n_templates) of logit-scale
template forecasts.}

\item{wts}{Numeric vector of ensemble weights (length n_templates).}

\item{se_mat}{Numeric matrix or NULL; per-template GAM SEs at forecast weeks
(same shape as \code{logit_mat}). Required for \code{"total"} mode.}

\item{spread_method}{Character; \code{"between"} or \code{"total"}.}
}
\value{
List with \code{spread} (numeric vector, length \code{nrow(logit_mat)})
  and \code{fallback_count} (integer).
}
\description{
\code{"between"} computes the weighted between-template standard deviation.
\code{"total"} adds weighted per-template SE\^2 (from \code{g_s_mu_se}) to
the between-template variance. When per-template SEs are all zero/missing for
a forecast week, that week falls back to between-only and the fallback count
is incremented.
}
\keyword{internal}
