% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkSeasonLength.R
\name{checkSeasonLength}
\alias{checkSeasonLength}
\title{Compute in-season length for each season}
\usage{
checkSeasonLength(dat, thresh = 0.05, inclusive = F)
}
\arguments{
\item{dat}{Data frame with one row per season-week. Must include columns
`season`, `weekF`, `y`, and `N`.}

\item{thresh}{Positivity threshold on the probability scale
(default `0.05`).}

\item{inclusive}{Logical; if `TRUE`, include both endpoints in the
length count (default `FALSE`).}
}
\value{
A tibble with one row per season and columns `season`,
  `start_week`, `end_week`, and `season_length_weeks`.
}
\description{
For each season in `dat`, finds the first week at which positivity
(`y / N`) crosses `thresh`, the first week after that where it drops
back below the threshold, and the gap between them. Used to validate
the threshold-based season definition against observed data.
}
