% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_training.R
\name{tune_m1}
\alias{tune_m1}
\title{Tune M1 alignment hyperparameters via LOSO grid search}
\usage{
tune_m1(
  allD,
  m0,
  m1 = NULL,
  loso_seasons = "all",
  grid = default_m1_grid(),
  n_cores = parallel::detectCores() - 1L,
  checkpoint_dir = NULL,
  verbose = TRUE,
  selection = NULL,
  manual_labels = NULL
)
}
\arguments{
\item{allD}{Multi-season surveillance data frame.}

\item{m0}{Output of \code{tune_m0()}. Must include \code{best_params}.}

\item{m1}{Output of \code{build_m1()}. Provides \code{m1_params}.}

\item{loso_seasons}{Which seasons to use as LOSO test folds.
\code{"all"} (default) tests every season; \code{"alternating"} tests
every other season.}

\item{grid}{Tuning grid. Default: \code{default_m1_grid()}.}

\item{n_cores}{Integer. Parallel cores.}

\item{checkpoint_dir}{Character. Directory for resumable checkpoints.
Uses a temp directory if \code{NULL}.}

\item{verbose}{Logical. Print progress.}

\item{selection}{Optional governed \code{page_season_selection}. When
supplied, \code{m0} must be frozen, only selected training seasons are
used, and the result is a \code{page_m1_tuning}.}

\item{manual_labels}{Optional named ignition-week vector in the M0 week
coordinate system. Defaults to labels stored in \code{m0}. M1 tuning
applies its historical one-week coordinate offset after resolving this
value.}
}
\value{
Output of \code{tune_m1_alignment()} -- a list with per-spec MAE
  scores and the best spec parameters.
}
\description{
Runs leave-one-season-out grid search over M1 alignment parameters using
\code{tune_m1_alignment()}. Supports resumable checkpoints.
}
