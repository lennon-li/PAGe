% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_bridge.R
\name{m1_walkforward_multi}
\alias{m1_walkforward_multi}
\title{Run M1 walk-forward for multiple seasons (parallelized)}
\usage{
m1_walkforward_multi(
  allD,
  ref,
  hyper,
  params,
  seasons = NULL,
  horizons = c(1L, 2L),
  eval_weeks = NULL,
  allow_scale = NULL,
  use_ci = TRUE,
  buffer_weeks = 0L,
  min_obs = 4L,
  curvature_ratio = 1,
  temperature = 0.25,
  rise_weight = 1,
  trough_weight = 0.1,
  peak_decay = 0.3,
  slope_weight = 0.5,
  slope_window = 4L,
  dynamic_temp = TRUE,
  dynamic_temp_pivot = 10L,
  top_k = NULL,
  blend_alpha = 1,
  parallel = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{allD}{Multi-season data frame.}

\item{ref}{Output from \code{estimateRef()}.}

\item{hyper}{Output from \code{learn_alignment_hyperparams()}.}

\item{params}{M0 detection params.}

\item{seasons}{Character vector of seasons to process.}

\item{horizons}{Forecast horizons (default \code{c(1L, 2L)}).}

\item{eval_weeks}{Optional; if NULL, determined per season from ignition.}

\item{allow_scale}{Passed through.}

\item{use_ci}{Passed through.}

\item{buffer_weeks}{Passed through.}

\item{min_obs}{Passed through.}

\item{curvature_ratio}{Passed through.}

\item{temperature, rise_weight, trough_weight, peak_decay}{Ensemble and
alignment-loss controls passed through.}

\item{slope_weight, slope_window}{Growth-rate similarity controls.}

\item{dynamic_temp, dynamic_temp_pivot}{Early-season temperature controls.}

\item{top_k, blend_alpha}{Template filtering and blending controls.}

\item{parallel}{Logical; use parallel via furrr (default TRUE).}

\item{verbose}{Logical; print progress (default TRUE).}
}
\value{
A tibble (stacked across seasons) with the same columns as
  \code{m1_walkforward_predictions()}.
}
\description{
Calls \code{m1_walkforward_predictions()} for each season, optionally
in parallel via \code{furrr::future_map()}.
}
