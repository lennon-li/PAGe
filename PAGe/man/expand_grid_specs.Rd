% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2_spec_grid.R
\name{expand_grid_specs}
\alias{expand_grid_specs}
\title{Expand a hyperparameter grid into Stage-2 spec objects (ALL hyperparams can vary)}
\usage{
expand_grid_specs(
  delta_grid = -3:3,
  Kr_grid = 1:6,
  T_grid = c("O", "S"),
  k_f_grid = c(6L, 8L, 10L),
  alpha_state = c(0.25),
  Kb_grid = c(0L, 1L),
  leads = c(1L, 2L),
  k_w_grid = c(8L),
  k_s_grid = c(0L),
  k_e_grid = c(6L),
  k_n_grid = c(6L),
  k_de_grid = c(0L),
  k_r_grid = c(0L),
  bs_week_grid = "ts",
  bs_fs_marginal_grid = "tp",
  bias_alpha_grid = c(0.4),
  bias_beta_grid = c(0),
  drop_unused_kf_for_nonS = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{delta_grid}{Integer vector.}

\item{Kr_grid}{Integer vector for ramp length.}

\item{T_grid}{Character vector in \code{c("O","S","N")}.}

\item{k_f_grid}{Integer vector (used only when \code{T=="S"}).}

\item{alpha_state}{Numeric vector in (0,1).}

\item{Kb_grid}{Integer vector for ignition buffer length.}

\item{leads}{Integer vector of horizons (typically fixed to \code{c(1L,2L)}).}

\item{k_w_grid, k_s_grid, k_e_grid, k_n_grid, k_de_grid}{Integer vectors for smooth basis sizes.}

\item{k_r_grid}{Integer vector for residual-smooth basis sizes.}

\item{bs_week_grid}{Character vector for week smooth basis.}

\item{bs_fs_marginal_grid}{Character vector for fs marginal basis.}

\item{bias_alpha_grid, bias_beta_grid}{Numeric vectors for Holt correction rates.}

\item{drop_unused_kf_for_nonS}{If TRUE, sets \code{k_f=NA} for \code{T!="S"}.}

\item{verbose}{Logical.}
}
\value{
List with \code{specs}, \code{grid}, and \code{n}.
}
\description{
Creates a cartesian product over all supplied grids and returns:
\itemize{
  \item a named list of \code{spec} objects (\code{$specs})
  \item a data.frame describing the grid (\code{$grid})
}
}
\details{
Special handling:
\itemize{
  \item \code{k_f} is only meaningful when \code{T=="S"}.
    For \code{T!="S"}, \code{k_f} is set to NA by default to avoid unnecessary expansion.
}
}
