% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_training.R
\name{build_m2}
\alias{build_m2}
\title{Build M2 forecast model via nested LOSO grid search}
\usage{
build_m2(
  allD,
  m0,
  m1,
  loso_seasons = "alternating",
  exclude_seas = "2015-16",
  holdout_season = "2025-26",
  grid = default_m2_grid(),
  bias_alpha = 0.05,
  bias_beta = 0,
  n_cores = parallel::detectCores() - 1L,
  checkpoint_dir = NULL,
  m1_artifact_path = NULL,
  fail_fast = TRUE,
  future_max_size = NULL,
  verbose = TRUE
)
}
\arguments{
\item{allD}{Multi-season surveillance data frame.}

\item{m0}{Output of \code{tune_m0()}. Must include \code{best_params}.}

\item{m1}{Output of \code{build_m1()}. Provides reference curve and params.}

\item{loso_seasons}{Which seasons to evaluate as LOSO test folds.
\code{"alternating"} (default) halves tuning time; \code{"all"} for
production quality. A character vector selects specific seasons.}

\item{exclude_seas}{Seasons to exclude from LOSO folds entirely.}

\item{holdout_season}{Prospective season excluded by default. Set to NULL
only after an explicit promotion release.}

\item{grid}{Tuning grid. Default: compact \code{default_m2_grid()} plan.
Per-row \code{bias_alpha} and \code{bias_beta} columns override their
scalar fallbacks.}

\item{bias_alpha, bias_beta}{Numeric. Backward-compatible Holt EMA scalar
fallbacks used when the grid omits the corresponding columns.}

\item{n_cores}{Integer. Parallel cores.}

\item{checkpoint_dir}{Character. Directory for Phase 2 checkpoint files.
Pass \code{NULL} to disable checkpointing.}

\item{m1_artifact_path}{Character. Optional path for the complete Phase 1
M1 LOSO artifact. When \code{NULL} and \code{checkpoint_dir} is supplied,
\code{m1_phase1.rds} is written inside that directory. A matching existing
artifact is reused; it is compacted in memory before M2 workers receive it.}

\item{fail_fast}{Logical. Stop on the first unsupported fold/spec instead of
converting model failures to missing scores (default \code{TRUE}).}

\item{future_max_size}{Numeric. Maximum size, in bytes, of globals exported
to \pkg{future} workers. When \code{NULL} (default), the limit is raised
only as needed for the prepared M1 cache, with a 2 GiB safety ceiling.}

\item{verbose}{Logical. Print progress.}
}
\value{
A list with \code{best_spec}, \code{best_spec_id}, \code{summary}
  (ranked by Bernoulli NLL), \code{scores}, \code{cv_results}, and
  \code{grid}. Pass \code{best_spec} to \code{train_m2()}.
}
\description{
Runs Phase 1 (M1 walk-forward cache per LOSO fold) and Phase 2 (frozen
GAM + Holt EMA bias grid search) to identify the best M2 spec.
Uses \code{loso_seasons = "alternating"} by default for fast demos;
switch to \code{"all"} for production tuning.
}
