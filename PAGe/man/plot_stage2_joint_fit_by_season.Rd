% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2_spec_grid.R
\name{plot_stage2_joint_fit_by_season}
\alias{plot_stage2_joint_fit_by_season}
\title{Prepare Stage-2 M1 features from aligned prospective data}
\usage{
plot_stage2_joint_fit_by_season(
  out_m1,
  feat_full,
  dat_raw = NULL,
  ign_hat_df = NULL,
  exclude_season_re = FALSE,
  exclude_newseason_terms = FALSE,
  facet_by_lead = TRUE,
  trim_preign = TRUE
)
}
\arguments{
\item{out_m1}{Fitted Stage-2 result containing \code{fit} and \code{spec}.}

\item{feat_full}{Feature data used for plotting.}

\item{dat_raw}{Optional raw observations.}

\item{ign_hat_df}{Optional estimated ignition weeks by season.}

\item{exclude_season_re}{Logical; omit the season random effect.}

\item{exclude_newseason_terms}{Logical; omit terms unavailable for new seasons.}

\item{facet_by_lead}{Logical; facet forecasts by horizon.}

\item{trim_preign}{Logical; omit pre-ignition rows.}
}
\value{
A data.table with original columns plus derived feature columns.
}
\description{
Computes standardized columns required by Stage-2 training/prediction:
\itemize{
  \item \code{y_now, N_now} from \code{y/N} (or \code{x/n})
  \item \code{d1_now, d2_now} from \code{d1_link/d2_link} if present, else \code{d1/d2}
  \item ignition week \code{ign_weekF} from \code{iWeek} or \code{ignition} or \code{ignD} fallback
  \item \code{logit_f_eff} = \code{omega(t_rel;Kr)} * template logit, where
    \code{omega(t;Kr)=clamp(t/Kr,0,1)} and \code{t_rel=weekF-ign_weekF}
  \item \code{z_ema} EWMA on observed logit positivity using \code{alpha_state}
  \item \code{logN_now} = log(N_now)
}
}
\details{
Requires helper functions already in your file:
\code{wrap_week()}, \code{ewma_recursive()}, \code{make_ref_logit_fun_from_template()}.
}
