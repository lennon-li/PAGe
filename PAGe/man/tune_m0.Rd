% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_training.R
\name{tune_m0}
\alias{tune_m0}
\title{Tune M0 ignition detection hyperparameters via LOSO grid search}
\usage{
tune_m0(
  allD,
  loso_seasons = "all",
  exclude = c("2011-12", "2015-16", "2020-21", "2021-22"),
  grid = .default_m0_grid(),
  manual_labels = .default_manual_labels(),
  flag_args = .default_flag_args(),
  n_cores = parallel::detectCores() - 1L,
  verbose = TRUE
)
}
\arguments{
\item{allD}{Multi-season surveillance data frame.}

\item{loso_seasons}{Which seasons to evaluate as LOSO test folds.
\code{"all"} (default) evaluates every season; \code{"alternating"}
uses every other season (removes non-selected from training too —
acceptable for quick demos). A character vector selects specific seasons.}

\item{exclude}{Character vector of seasons to permanently exclude.}

\item{grid}{Tuning grid as a data frame. Default: \code{.default_m0_grid()}.}

\item{manual_labels}{Named integer vector of manual ignition labels.}

\item{flag_args}{List of ignition-flagging parameters.}

\item{n_cores}{Integer. Parallel cores (default: all minus 1).}

\item{verbose}{Logical. Print progress.}
}
\value{
A list with \code{best_params}, \code{tuning} (full
  \code{loso_M0v2()} output), \code{aligned}, \code{seasons_used},
  \code{manual_labels}, and \code{flag_args}. Pass directly to
  \code{build_m1()}, \code{build_m2()}, and \code{train_m2()}.
}
\description{
Runs leave-one-season-out grid search over M0 detection parameters using
\code{loso_M0v2()}. The 36-spec grid matches the production tuning run.
}
