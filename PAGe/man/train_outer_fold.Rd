% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested_season_evaluation.R
\name{train_outer_fold}
\alias{train_outer_fold}
\title{Train a nested-tuned PAGe kit}
\usage{
train_outer_fold(
  data,
  holdout = NULL,
  exclude = .default_nested_exclusions(),
  manual_labels = NULL,
  timing_labels = NULL,
  m0_grid = .default_m0_grid(),
  m0_flag_args = .default_flag_args(),
  m1_grid = default_m1_grid(),
  m1_params = .default_m1_params(),
  m2_grid = m2_subset_grid(),
  m2_family = m2_subset_family(),
  early_weight = 2,
  early_max_t_since = 12,
  pre_ignition_weight = 0,
  late_weight = 1,
  score_scale = c("equal_week", "test_count"),
  min_gain = 0.0012,
  min_gain_by_horizon = c(`2` = 0.002),
  confidence = 0.95,
  max_season_degradation = 0,
  m1_min_gain = 0.05,
  m1_hard_caps = default_m1_hard_caps(),
  m1_prefer_simpler = TRUE,
  m2_min_nll_gain = default_m2_nll_gain_caps(),
  max_boundary_rounds = .default_boundary_round_limits(),
  m0_expansion_steps = NULL,
  m1_expansion_steps = .default_m1_expansion_steps(),
  m2_expansion_steps = NULL,
  m2_expansion_increment = .default_m2_expansion_increment(),
  min_training_seasons = 2L,
  n_cores = max(1L, parallel::detectCores() - 1L),
  checkpoint_dir = NULL,
  artifact_dir = NULL,
  verbose = TRUE,
  timing_mode = c("legacy", "fractional")
)
}
\arguments{
\item{data}{Canonical multi-season surveillance data.}

\item{holdout}{Optional character scalar identifying the outer-held-out
season. Use `NULL` only to fit the final kit after the evaluation procedure
has been fixed.}

\item{exclude}{Character vector of fixed exclusions. Defaults to the
protocol exclusions `"2011-12"`, `"2015-16"`, `"2020-21"`, and
`"2021-22"`.}

\item{manual_labels}{Optional named ignition-week labels. Any label for
`holdout` is removed before tuning.}

\item{timing_labels}{Optional timing-v2 label object or list of objects.
The earlier ignition week is converted to the existing M0/M1 contract;
peak weeks and the normalized uncertainty pairs are retained in the
training result and artifacts. Any object for `holdout` is removed before
tuning, while the complete supplied input is retained for provenance.
Cannot be combined with `manual_labels`.}

\item{m0_grid, m1_grid, m2_grid}{Initial stage tuning grids.}

\item{m0_flag_args}{Named M0 ignition flag arguments forwarded to
`tune_m0()` and `fit_m0()`. Defaults to the package detector defaults
(`.default_flag_args()`).}

\item{m1_params}{Named baseline M1 settings that `tune_m1()` searches
around. Defaults to `.default_m1_params()`.}

\item{m2_family}{M2 model family forwarded to `tune_m2()` and `fit_m2()`.
Defaults to `m2_subset_family()` (`"offset_subset_v1"`).}

\item{early_weight}{Weight for target-relative weeks zero through
`early_max_t_since`.}

\item{early_max_t_since}{Last target-relative week receiving `early_weight`.}

\item{pre_ignition_weight}{Phase weight for pre-ignition targets. The
manuscript protocol is `0`.}

\item{late_weight}{Phase weight for targets later than `early_max_t_since`.
The manuscript protocol is `1`.}

\item{score_scale}{M2 tuning scale. The manuscript primary is `equal_week`;
`test_count` is available as a sensitivity analysis.}

\item{min_gain}{Overall M2 NLL gain required over M1.}

\item{min_gain_by_horizon}{Optional named horizon-specific gain floors.}

\item{confidence}{One-sided season-level confidence used by the adoption
rule.}

\item{max_season_degradation}{Largest permitted season-specific M2 NLL loss.}

\item{m1_min_gain}{Practical M1 improvement needed for added complexity.}

\item{m1_hard_caps}{Governed M1 hard caps.}

\item{m1_prefer_simpler}{Logical; prefer the simplest M1 candidate within
`m1_min_gain` of the best. Defaults to `TRUE`.}

\item{m2_min_nll_gain}{Parameter-specific M2 boundary gain caps.}

\item{max_boundary_rounds}{Named positive integer vector for M0, M1, and M2.
Defaults to `c(M0 = 5L, M1 = 4L, M2 = 6L)`.}

\item{m0_expansion_steps}{Optional named adjacent expansion steps forwarded
to `boundary_action_plan()` for M0. `NULL` uses the grid-derived spacing.}

\item{m1_expansion_steps}{Named positive adjacent expansion steps forwarded
to `boundary_action_plan()` for M1. Defaults to `c(k_ref = 5,
slope_weight = 4)`.}

\item{m2_expansion_steps}{Optional named adjacent expansion steps forwarded
to `boundary_action_plan()` for M2. `NULL` uses the grid-derived spacing.}

\item{m2_expansion_increment}{Additional grid rows permitted per M2
boundary expansion round. Defaults to `12L`.}

\item{min_training_seasons}{Minimum number of outer-training seasons
required before fitting. Defaults to `2L`, the leave-one-season-out
minimum.}

\item{n_cores}{Number of workers supplied to stage tuners. Defaults to one
fewer than `parallel::detectCores()`.}

\item{checkpoint_dir}{Optional resumable checkpoint directory.}

\item{artifact_dir}{Optional directory receiving grids, tuning objects,
boundary reports, decisions, frozen stages, and the final kit.}

\item{verbose}{Logical progress flag.}
}
\value{
A `page_outer_training` object containing the season selection,
  tuning results, boundary histories, adoption evidence, frozen stages, and
  assembled kit.
}
\description{
Runs the first two levels of the manuscript evaluation. When `holdout` is
supplied, that season is kept outside all fitting and selection. Within the
remaining seasons, the stage tuners perform leave-one-season-out
walk-forward evaluation, boundary grids are expanded until settled, and the
phase-weighted M2-versus-M1 adoption rule is applied before a frozen kit is
assembled. `holdout = NULL` is reserved for the final post-evaluation fit on
all eligible seasons.
}
