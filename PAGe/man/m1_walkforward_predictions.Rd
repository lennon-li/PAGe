% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_bridge.R
\name{m1_walkforward_predictions}
\alias{m1_walkforward_predictions}
\title{Run M1 walk-forward for one season and collect predictions at target weeks}
\usage{
m1_walkforward_predictions(
  seasonD,
  ref,
  hyper,
  ign_out = NULL,
  params = NULL,
  horizons = c(1L, 2L),
  eval_weeks = NULL,
  allow_scale = NULL,
  use_ci = TRUE,
  buffer_weeks = 0L,
  min_obs = 4L,
  curvature_ratio = 1,
  temperature = 0.25,
  rise_weight = 1,
  trough_weight = 0.1,
  peak_decay = 0.3,
  slope_weight = 0.5,
  slope_window = 4L,
  dynamic_temp = TRUE,
  dynamic_temp_pivot = 10L,
  top_k = NULL,
  blend_alpha = 1
)
}
\arguments{
\item{seasonD}{Data frame for ONE season (all weeks). Must have
columns \code{weekF}, \code{y}, and either \code{N} or \code{neg}.}

\item{ref}{Output from \code{estimateRef()} (pre-computed from training data).}

\item{hyper}{Output from \code{learn_alignment_hyperparams()}.}

\item{ign_out}{Pre-computed output from \code{run_ignition_weekly()}.
If NULL, must supply \code{params} to run M0 internally.}

\item{params}{M0 detection params. Used only if \code{ign_out} is NULL.}

\item{horizons}{Integer vector of forecast horizons (default \code{c(1L, 2L)}).}

\item{eval_weeks}{Optional integer vector of weekF values to evaluate at.
If NULL, evaluates from ignition lock through end of season.}

\item{allow_scale}{Passed to \code{run_alignment_prospective()}.}

\item{use_ci}{Logical; peak CI control (default TRUE).}

\item{buffer_weeks}{Integer; peak buffer (default 0L).}

\item{min_obs}{Integer; minimum rows for alignment (default 4L).}

\item{curvature_ratio}{Numeric; delta curvature gate (default 1.0).}

\item{temperature, rise_weight, trough_weight, peak_decay}{Ensemble and
alignment-loss controls.}

\item{slope_weight, slope_window}{Growth-rate similarity controls.}

\item{dynamic_temp, dynamic_temp_pivot}{Early-season temperature controls.}

\item{top_k, blend_alpha}{Template filtering and blending controls.}
}
\value{
A tibble with columns:
\describe{
  \item{season}{Season label}
  \item{eval_weekF}{The weekF at which M1 was run (data up to this week)}
  \item{target_weekF}{The weekF for which M1 predicts (eval_weekF + h)}
  \item{h}{Forecast horizon (1 or 2)}
  \item{m1_p_hat}{M1's predicted positivity at target week}
  \item{m1_p_lo}{M1's lower PI at target week}
  \item{m1_p_hi}{M1's upper PI at target week}
  \item{m1_tau}{M1's shift parameter at eval_weekF}
  \item{m1_delta}{M1's dilation parameter at eval_weekF}
  \item{m1_state}{M1 state: "aligning" or "post_peak"}
}
}
\description{
For each evaluation week, runs \code{run_alignment_prospective()} and
extracts M1's template-based prediction at each forecast target week
(weekF + h). Returns a tidy tibble suitable for joining to M2 training data.
}
