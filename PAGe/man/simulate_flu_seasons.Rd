% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_flu_seasons}
\alias{simulate_flu_seasons}
\title{Simulate synthetic flu seasons for package examples}
\usage{
simulate_flu_seasons(S = 10, weeks = 1:52, seed = 2025)
}
\arguments{
\item{S}{Integer number of seasons to simulate (default 10).}

\item{weeks}{Integer vector of within-season week indices to generate
(default \code{1:52}).}

\item{seed}{Integer random seed for reproducibility (default 2025).}
}
\value{
A data frame with columns \code{season} (factor), \code{newWeek},
  \code{y} (positives), and \code{neg} (negatives).
}
\description{
Generates \code{S} synthetic influenza seasons from a Gaussian-bump
template with random per-season amplitude, intercept, and timing
(\code{tau}) variation. Counts are drawn from a Binomial distribution with
weekly \code{n} sampled uniformly from [600, 1500].
}
