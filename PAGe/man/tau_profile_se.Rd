% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m1_fit.R
\name{tau_profile_se}
\alias{tau_profile_se}
\title{Estimate tau and its profile standard error}
\usage{
tau_profile_se(
  currentD,
  g_ref,
  allow_scale = FALSE,
  h = 0.001,
  tau0 = 0,
  tau_bounds = c(-12, 12)
)
}
\arguments{
\item{currentD}{Data frame for one season with columns \code{newWeek},
\code{y} (positives), and \code{neg} (negatives).}

\item{g_ref}{Reference curve function on the logit scale,
\code{g_ref(u) = logit(p_ref(u))}.}

\item{allow_scale}{Logical; if \code{TRUE} fits an intercept + slope
(\code{a + b * g_ref}) rather than offset-only (default \code{FALSE}).}

\item{h}{Numeric step size for numerical second derivative (default
\code{1e-3}).}

\item{tau0}{Numeric starting value for the search interval centre
(default 0).}

\item{tau_bounds}{Numeric vector of length 2; hard limits for \code{tau}
(default \code{c(-12, 12)}).}
}
\value{
A list with \code{tau_hat} (numeric) and \code{se_tau}
  (numeric, \code{NA} when the Hessian is non-positive).
}
\description{
Fits the temporal alignment shift \code{tau} by 1-D golden-section
optimisation over profile log-likelihood (marginalising over the intercept
via GLM), then approximates the standard error of \code{tau_hat} from the
numerical second derivative of the profile \emph{deviance}.
}
\keyword{internal}
