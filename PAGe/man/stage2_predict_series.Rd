% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2_runtime.R
\name{stage2_predict_series}
\alias{stage2_predict_series}
\title{Produce per-snapshot Stage-2 forecast series (h1/h2) on the target-week axis}
\usage{
stage2_predict_series(
  pp,
  stage2_fit,
  which = c("all", "latest"),
  horizons = c(1L, 2L),
  alpha_state = NULL,
  ref_col = "template_fit_shift",
  exclude_season_re = TRUE,
  interval = c("pi", "ci"),
  level = 0.95,
  pi_B = 2000L,
  pi_seed = 1L,
  date_step_days = 7L
)
}
\arguments{
\item{pp}{Output of \code{build_stage2_pseudo_prospective_list()} (list with \code{meta} and \code{df})
or a compatible list of snapshot data.frames.}

\item{stage2_fit}{A fitted \pkg{mgcv} \code{gam}/\code{bam} Stage-2 model.}

\item{which}{Which snapshots to process: \code{"all"} (default) or \code{"latest"}.}

\item{horizons}{Integer vector of horizons to include (default \code{c(1L,2L)} -> \code{h1,h2}).}

\item{alpha_state}{Numeric in (0,1). If \code{z_ema} is missing, it is computed as an EWMA
on the logit scale using this alpha. Defaults to \code{pp$meta$alpha_state} if present, else 0.3.}

\item{ref_col}{Character. Column name used as background reference curve (default \code{"template_fit_shift"}).}

\item{exclude_season_re}{Logical. If TRUE (default), excludes \code{s(season)} during prediction.}

\item{interval}{Interval type, prediction or confidence.}

\item{level}{Confidence level for intervals (default 0.95).}

\item{pi_B}{Number of prediction-interval simulations.}

\item{pi_seed}{Random seed for interval simulation.}

\item{date_step_days}{Integer days per week when imputing missing dates (default 7).}
}
\value{
If \code{which="latest"}, returns a single data.frame.
  If \code{which="all"}, returns a list of data.frames (one per snapshot) in the same order as input snapshots.
}
\description{
Takes pseudo-prospective snapshots produced by
\code{build_stage2_pseudo_prospective_list()} and applies a fitted Stage-2 model
to produce forecasts aligned to the *target* week:
\itemize{
\item \code{h1} predictions are placed at \code{weekF_target = weekF_origin + 1}
\item \code{h2} predictions are placed at \code{weekF_target = weekF_origin + 2}
}
}
\details{
The returned time series for each snapshot contains:
\itemize{
\item \code{weekF}, \code{newWeek}, \code{date}
\item \code{p_obs}: observed probability (masked beyond the as-of week)
\item \code{p_true}: retrospective truth (if present in snapshots)
\item \code{p_ref}: reference/template curve (from \code{ref_col})
\item \code{p_hat_h1}, \code{p_lo_h1}, \code{p_hi_h1}
\item \code{p_hat_h2}, \code{p_lo_h2}, \code{p_hi_h2}
\item \code{asof_weekF}: the as-of origin week for that snapshot
}

Uncertainty bands are computed as link-scale confidence intervals for the mean,
transformed back to the response scale via \code{plogis()}.
}
