% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_training.R
\name{train_m2}
\alias{train_m2}
\title{Fit the M2 production GAM on all training seasons}
\usage{
train_m2(
  allD,
  m0,
  m1,
  best_spec = NULL,
  exclude = c("2011-12", "2015-16", "2020-21", "2021-22"),
  verbose = FALSE
)
}
\arguments{
\item{allD}{Multi-season surveillance data frame.}

\item{m0}{Output of \code{tune_m0()}. Must include \code{best_params}.}

\item{m1}{Output of \code{build_m1()}. Provides \code{ref} and \code{hyper}.}

\item{best_spec}{Stage-2 spec from \code{build_m2()$best_spec} or
\code{stage2_make_spec()}. When \code{NULL}, uses the locked deployed v16
specification for a production-data refresh without retuning.}

\item{exclude}{Character vector of seasons to exclude from training.
Default excludes permanent invalid seasons and 2015-16. Note: 2025-26
is kept (production training uses the current season).}

\item{verbose}{Logical. Print progress.}
}
\value{
A list with \code{fit} (GAM), \code{feature_ranges}, \code{m1_train_preds},
  \code{spec}, \code{training_seasons}, and \code{spec_version}. Pass to
  \code{assemble_kit()}.
}
\description{
Trains the Stage-2 GAM on all non-excluded seasons using the best spec
from \code{build_m2()}. Runs M1 walk-forward predictions before fitting.
}
