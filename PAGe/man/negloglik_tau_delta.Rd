% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m1_fit.R
\name{negloglik_tau_delta}
\alias{negloglik_tau_delta}
\title{Negative log-likelihood for tau/delta alignment}
\usage{
negloglik_tau_delta(par, t, y, n, gfun, allow_scale = TRUE, lam = 0.1, w = n)
}
\arguments{
\item{par}{Numeric vector of parameters. When `allow_scale = TRUE`:
`c(tau, a, b, delta)`; otherwise `c(tau, a, delta)` with `b` fixed to 1.}

\item{t}{Numeric vector of observation times (`newWeek`).}

\item{y}{Integer vector of weekly positive counts.}

\item{n}{Integer vector of weekly total tests.}

\item{gfun}{Reference curve function on the logit scale.}

\item{allow_scale}{Logical; if `TRUE`, estimate slope `b`
(default `TRUE`).}

\item{lam}{Ridge penalty on `delta^2` (default `0.1`).}

\item{w}{Numeric vector of per-observation weights
(default `n`, i.e. weight by total tests).}
}
\value{
A numeric scalar; the penalised binomial negative log-likelihood.
}
\description{
Binomial negative log-likelihood for the time-alignment model
`logit(p) = a + b * g(u)` with `u = (t - tau) / (1 + delta)`,
with an optional ridge penalty on `delta`. Used as the objective inside
the nonlinear optimiser in [align_forecast_pipeline_dilate()].
}
