% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_plan.R
\name{plan_training}
\alias{plan_training}
\title{Plan a governed PAGe training run without fitting or launching workers}
\usage{
plan_training(
  allD,
  mode = c("refresh", "retune"),
  previous_results = NULL,
  exclude = c("2011-12", "2015-16", "2020-21", "2021-22"),
  prospective_holdout = "2025-26",
  promotion = NULL,
  loso_seasons = "all",
  n_cores = parallel::detectCores() - 1L,
  checkpoint_dir = NULL,
  m0_grid = .default_m0_grid(),
  m1_grid = default_m1_grid(),
  m2_grid = NULL,
  max_m2_finalists = 6L,
  max_m2_specs = 64L,
  m1_hard_caps = default_m1_hard_caps()
)
}
\arguments{
\item{allD}{Multi-season surveillance data.}

\item{mode}{One of \code{"refresh"} or \code{"retune"}.}

\item{previous_results}{Optional prior M2 tuning result used to plan its grid.}

\item{exclude}{Seasons excluded from training and fitting.}

\item{prospective_holdout}{Season held out until promotion evidence releases it.}

\item{promotion}{Optional verified promotion evidence.}

\item{loso_seasons}{LOSO fold selector: \code{"all"}, \code{"alternating"},
or an explicit character vector.}

\item{n_cores}{Requested worker count.}

\item{checkpoint_dir}{Optional parent checkpoint directory; it is reported but
not created.}

\item{m0_grid, m1_grid}{Optional explicit M0/M1 grids.}

\item{m2_grid}{Optional explicit M2 grid; otherwise \code{plan_m2_grid()} is
used.}

\item{max_m2_finalists, max_m2_specs}{Bounds for an automatically planned M2 grid.}

\item{m1_hard_caps}{M1 hard-cap policy.}
}
\value{
A \code{page_training_plan} containing season selection, grids,
  support audit, cap policy, and resource estimates.
}
\description{
Normalizes the season sets, grids, M1 caps, support checks, and resource
settings that a subsequent \code{train_pipeline()} call would use. This is
a dry run: it does not fit models, create checkpoints, or write artifacts.
}
