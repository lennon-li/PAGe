% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2_spec_grid.R
\name{stage2_make_spec}
\alias{stage2_make_spec}
\title{Create a Stage-2 training specification (hyperparameters + derived objects)}
\usage{
stage2_make_spec(
  delta = 0L,
  Kr = 3L,
  k_f = 6L,
  alpha_state = 0.3,
  Kb = 0L,
  leads = c(1L, 2L),
  T = c("S", "O", "N"),
  template_mode = NULL,
  use_ramp = NULL,
  k_e = 6L,
  k_n = 0L,
  k_de = 0L,
  k_r = 0L,
  k_w = 0L,
  k_s = 0L,
  k_sp = 0L,
  bs_week = "ts",
  bs_fs_marginal = "tp",
  use_season_re = TRUE,
  lambda_w = 0,
  w_floor = 0.05,
  anchorWeek = 20L,
  bias_alpha = 0.2,
  bias_beta = 0,
  K = NULL,
  pre_buffer = NULL
)
}
\arguments{
\item{delta}{Integer template shift in weeks.}

\item{Kr}{Integer ramp length (>=1). Kr=1 means "immediate" ramp after ignition week.}

\item{k_f}{Integer basis size for template smooth (only used when \code{T="S"}).}

\item{alpha_state}{Numeric EWMA decay in (0,1) used to compute \code{z_ema}.}

\item{Kb}{Integer buffer (weeks before ignition) included in training window:
training rows satisfy \code{weekF >= ign_weekF - Kb}.}

\item{leads}{Integer horizons (usually \code{c(1L,2L)}).}

\item{T}{Template entry mode:
\code{"S"} = smooth term; \code{"O"} = offset; \code{"N"} = no template.}

\item{template_mode}{Back-compat alias of \code{T}: "smooth"/"offset"/"none".}

\item{use_ramp}{Deprecated. If \code{FALSE}, forces \code{Kr=1}.}

\item{k_de}{Integer basis size for dz_ema smooth term. 0L disables it.}

\item{k_r, k_sp}{Integer basis sizes for residual and alignment-spread smooths.}

\item{k_w, k_s, k_e, k_n}{Integer basis sizes for smooth terms.
Set any to 0L to disable the corresponding term.}

\item{bs_week}{Basis name for week smooths (typical: "ts").}

\item{bs_fs_marginal}{Marginal basis used by factor-smooth \code{bs="fs"} via \code{xt=list(bs=...)}.}

\item{use_season_re}{Back-compat flag (season RE is always included).}

\item{lambda_w, w_floor}{Training time-decay rate and minimum weight.}

\item{anchorWeek}{Reference-curve ignition anchor week.}

\item{bias_alpha, bias_beta}{Holt level and trend correction rates.}

\item{K}{Deprecated alias of \code{Kr}.}

\item{pre_buffer}{Deprecated alias of \code{Kb}.}
}
\value{
A list \code{spec} containing hyperparameters plus:
\itemize{
  \item \code{spec$formula} joint model formula
  \item \code{spec$exclude_newseason} terms to exclude for new-season prediction
  \item \code{spec$best_row} small data.frame for printing
}
}
\description{
Builds a spec list that defines:
\itemize{
  \item template shift \code{delta}
  \item template ramp length \code{Kr}
  \item training buffer window \code{Kb} (weeks before ignition)
  \item spline basis sizes \code{k_*}
  \item template entry mode \code{T} (smooth/offset/none)
  \item derived: \code{spec$formula} and \code{spec$exclude_newseason}
}
}
\details{
This function expects two project helpers to exist:
\itemize{
  \item \code{stage2_build_joint_formula(spec)}
  \item \code{stage2_exclude_newseason(spec)}
}
}
