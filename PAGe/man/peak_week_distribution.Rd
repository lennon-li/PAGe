% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retired.R
\name{peak_week_distribution}
\alias{peak_week_distribution}
\title{Peak week (newWeek) per season + suggested peak-rule parameters}
\usage{
peak_week_distribution(
  theD,
  value_col = NULL,
  k_for_drop = 2L,
  drop_prob = 0.5,
  abs_drop_prob = 0.5,
  max_week_prob = 0.95
)
}
\arguments{
\item{theD}{historical data with at least:
season, newWeek, and either p or (y, neg).}

\item{value_col}{column to define the peak. If NULL, uses p if present,
otherwise computes p = y/(y+neg).}

\item{k_for_drop}{integer: how many weeks after the peak to look at
when estimating the typical drop (also used as suggested min_consec_below).}

\item{drop_prob}{quantile of the *relative* drop to use, e.g. 0.5 for median.}

\item{abs_drop_prob}{quantile of the *absolute* drop to use for min_abs_drop.}

\item{max_week_prob}{quantile of peak_newWeek to use for max_week (e.g. 0.95).}
}
\value{
A list with:
  \itemize{
    \item \code{peaks}: tibble(season, peak_newWeek, peak_value)
    \item \code{suggested}: list with
      \itemize{
        \item \code{min_week}
        \item \code{max_week}
        \item \code{drop_frac}
        \item \code{min_abs_drop}
        \item \code{min_consec_below}
      }
  }
}
\description{
Peak week (newWeek) per season + suggested peak-rule parameters
}
