% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boundary_actions.R
\name{boundary_action_plan}
\alias{boundary_action_plan}
\title{Build the next action for a tuning result}
\usage{
boundary_action_plan(
  tuning,
  stage = c("M0", "M1", "M2"),
  hard_caps = NULL,
  selection_method = c("min_nll", "one_se", "pareto"),
  steps = NULL,
  max_specs = NULL
)
}
\arguments{
\item{tuning}{A `page_m0_tuning`, `page_m1_tuning`, or `page_m2_tuning` result.}

\item{stage}{One of `"M0"`, `"M1"`, or `"M2"`.}

\item{hard_caps}{M1 hard-cap policy. Defaults to `default_m1_hard_caps()`.}

\item{selection_method}{M2 selection rule.}

\item{steps}{Optional adjacent expansion steps passed to
`expand_tuning_grid()`.}

\item{max_specs}{Optional expanded-grid row cap.}
}
\value{
A `page_boundary_action_plan` containing raw/final selections,
  reports, unresolved axes, and `next_grid` (or `NULL` when settled).
}
\description{
Combines the raw optimizer result, governed candidate selection, boundary
reports, and (when needed) the next resumable grid. This is read-only and
does not score or mutate the supplied tuning object.
}
