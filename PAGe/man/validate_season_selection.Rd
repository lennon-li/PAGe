% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage_contracts.R
\name{validate_season_selection}
\alias{validate_season_selection}
\title{Validate and normalize a season selection}
\usage{
validate_season_selection(
  data,
  training_seasons,
  exclude_seasons = character(0),
  holdout_seasons = character(0),
  application_seasons = character(0)
)
}
\arguments{
\item{data}{A canonical surveillance data frame (must contain a
\code{season} column).}

\item{training_seasons}{Character vector of seasons used for training.
Must be non-empty.}

\item{exclude_seasons}{Character vector of seasons permanently excluded.}

\item{holdout_seasons}{Character vector of holdout evaluation seasons.}

\item{application_seasons}{Character vector of prospective application
seasons.}
}
\value{
A \code{page_season_selection} list with sorted, validated
  season vectors and the full set of data seasons.
}
\description{
Checks that all requested season identifiers exist in the canonical data,
are scalar character values without duplicates, and that the training,
exclusion, holdout, and application sets are mutually disjoint. Returns a
normalized \code{page_season_selection} object with deterministic
(sorted) ordering.
}
