% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/realtime_sources.R
\name{page_forecast_now}
\alias{page_forecast_now}
\title{Produce a real-time forecast from a frozen kit and any supported source}
\usage{
page_forecast_now(
  kit,
  source = NULL,
  season = NULL,
  walk_start = 5L,
  manual_ign_week = NA_integer_,
  timing_mode = c("fractional", "legacy"),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{kit}{A frozen PAGe kit.}

\item{source}{Passed to \code{page_load_surveillance()}; \code{NULL}
(default) reads the live PHO ORVT feed.}

\item{season}{Optional PAGe season label.}

\item{walk_start}{Minimum M1 evaluation week (default 5). The effective
start is \code{max(walk_start, locked ignition week)}.}

\item{manual_ign_week}{Optional integer week overriding M0 detection.}

\item{timing_mode}{\code{"fractional"} (default) or \code{"legacy"}.}

\item{verbose}{Print stage progress.}

\item{...}{Passed to \code{page_load_surveillance()}.}
}
\value{
A list with \code{forecast} (latest-origin h1/h2 rows, possibly
  zero-row), \code{ignition} (status scalars), \code{result} (the full
  \code{run_prospective_pipeline()} object) and \code{source} (provenance).
}
\description{
Thin operational wrapper: resolve the source, confirm the kit is frozen,
run the frozen walk-forward, and return the pipeline result together with
the resolved provenance and a tidy latest-origin summary. Pre-ignition
seasons legitimately return zero forecast rows; that is a valid result and
not an error.
}
