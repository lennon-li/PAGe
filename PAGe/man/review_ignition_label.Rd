% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ignition_label_workflow.R
\name{review_ignition_label}
\alias{review_ignition_label}
\title{Review a season before assigning its retrospective ignition label}
\usage{
review_ignition_label(
  data,
  season = NULL,
  smooth_window = 3L,
  p_threshold = 0.01,
  candidate_window = NULL,
  peak_tolerance = 1e-12,
  confidence = 0.95
)
}
\arguments{
\item{data}{Canonical surveillance data containing \code{season},
\code{weekF}, \code{y}, and \code{N} (or \code{neg}). It must contain one
season after optional filtering; use \code{season} to select one season
from multi-season data.}

\item{season}{Optional season identifier. Required when \code{data} contains
more than one season.}

\item{smooth_window}{Positive integer used for the centred moving average.}

\item{p_threshold}{Positivity threshold shown in the plot and used for
candidate summaries.}

\item{candidate_window}{Optional two-integer weekF range in which candidates
may be considered. The observed range is used when omitted.}

\item{peak_tolerance}{Non-negative tolerance for treating observed positivity
values as tied peak candidates.}

\item{confidence}{Confidence level for Wilson intervals.}
}
\value{
An object of class \code{page_ignition_review} with \code{signals},
  \code{summary}, \code{candidates}, \code{plot}, and a \code{provenance}
  list. Pass it to \code{finalize_ignition_label()} after visual review.
}
\description{
Creates a transparent, reproducible review object for one canonical PAGe
season. The object contains week-level positivity signals, Wilson intervals,
simple candidate summaries, and a ggplot object for visual inspection. No
label is assigned by this function and the input is never modified.
}
