% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m0_training.R
\name{plot_cls_models_by_season}
\alias{plot_cls_models_by_season}
\title{Plot classifier scores by season and model}
\usage{
plot_cls_models_by_season(
  ign_fit,
  score_cols = c(base = "p_cls_p", slope = "p_cls_slope_pop", fs = "p_cls_fs_pop"),
  x_col = "weekF",
  x_max = 30L,
  y_max = 0.3,
  lead = 1L,
  thr = NULL,
  use_plotly = TRUE,
  ncol = NULL
)
}
\arguments{
\item{ign_fit}{Output from [fitIgnition()] (list with \code{$data}) or a data.frame.}

\item{score_cols}{Named character vector of score columns to plot (name used as model label).}

\item{x_col}{Week column to plot on x-axis. Default \code{"weekF"}.}

\item{x_max}{Plot only weeks \code{<= x_max}. Default \code{30}.}

\item{y_max}{Y-axis maximum. Default \code{0.3}.}

\item{lead}{Label shift used in [fitIgnition()] (dotted line at \code{iWeek_true - lead}). Default \code{1}.}

\item{thr}{Optional numeric vector of horizontal reference lines.}

\item{use_plotly}{If TRUE returns \code{plotly::ggplotly(p)}. Default TRUE.}

\item{ncol}{Optional integer; number of columns in \code{facet_wrap()}.}
}
\value{
A ggplot object, or plotly object if \code{use_plotly=TRUE}.
}
\description{
Visualization helper for Stage-1 classifier scores produced by [fitIgnition()].
It will plot any score columns that exist in \code{ign_fit$data}; missing score columns
are silently dropped (so it works when only the base model is fitted).
}
\details{
Each facet corresponds to one \code{model | season} panel.
Two vertical reference lines are drawn per panel:
\itemize{
  \item dashed: truth ignition week \code{iWeek_true = min(weekF[phase==1])}.
  \item dotted: label endpoint week \code{iWeek_true - lead} used in classifier training.
}
Horizontal dotted lines can be added via \code{thr} to visually assess score thresholds.
}
