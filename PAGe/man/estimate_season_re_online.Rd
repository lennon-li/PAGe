% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2_training.R
\name{estimate_season_re_online}
\alias{estimate_season_re_online}
\title{Estimate season random effect causally from accumulated observations}
\usage{
estimate_season_re_online(fit, obs_df, ex_terms = NULL, lambda_re = 1)
}
\arguments{
\item{fit}{Fitted mgcv GAM with a season random effect.}

\item{obs_df}{Data frame of current-season observations with columns
\code{y}, \code{N}, and all covariates required by \code{fit}.}

\item{ex_terms}{Character vector of additional terms to exclude.}

\item{lambda_re}{Shrinkage strength; default 1 (one pseudo-observation at 0).}
}
\value{
Numeric scalar (the shrinkage RE estimate on the logit scale).
}
\description{
Computes a shrinkage scalar for the current season's intercept shift on the
logit scale using only observations up to the current week. The estimate
equals the mean logit-scale residual (observed minus GAM prediction without
season RE), shrunk toward zero by \code{lambda_re}. Adding this scalar to
\code{bias_logit} in \code{m2_predict_one()} replaces the hard exclusion of
\code{s(season)} while remaining causally safe.
}
