---
title: "M2 forecast training"
execute:
  eval: false
---

::: {.callout-caution}
This is a source-level training example, not verified production evidence.
Private historical metrics and promoted artifacts are not distributed with the
package. Complete acceptance, fixed-spec refresh, and immutable promotion
before calling any fit a deployment.
:::

M2 forecasts positivity one and two weeks ahead with a binomial GAM. Its
covariates include current surveillance features and M1 outputs constructed at
the same issue week.

## Fit and specify

```{r}
library(PAGe)

spec <- stage2_make_spec(
  delta = 0, Kr = 1, k_f = 4, k_e = 2,
  alpha_state = 0.15, k_r = 0, k_de = 0, k_sp = 6,
  bias_alpha = 0.05, bias_beta = 0
)
m2 <- train_m2(historical, m0, m1, best_spec = spec)
```

These are the coded v16 incumbent values, not proof of a deployed kit. Use
walk-forward LOSO before proposing a different specification, then complete the
governed holdout and promotion sequence.

Historical M2 training uses leave-one-season-out folds with fold-specific
reference and M1 feature construction. M0 and M1 hyperparameters were selected
before the M2 folds, so this is conditional M2 validation rather than fully
nested selection of the entire pipeline.

## Frozen model and online correction

The frozen runtime uses a level-only correction with base `bias_alpha=0.05`,
`bias_beta=0`, and high `bias_alpha=0.7` after two same-sign transitions (the
third consecutive same-sign residual). The season-specific effect is estimated
online from eligible post-ignition observations. Both updates must use errors
whose targets are already observed at the issue date.

Use Bernoulli negative log likelihood as the primary probabilistic score and
retain horizon-specific calibration and absolute-error summaries. Pooling only
across all weeks can conceal poor performance near ignition or the seasonal
peak.
