# Freeze an M2 draft artifact

Freeze an M2 draft artifact

## Usage

``` r
freeze_m2(fit, tuning = NULL, ...)
```

## Arguments

- fit:

  A `page_m2_fit`.

- tuning:

  Optional `page_m2_tuning` to validate.

- ...:

  Reserved.

## Value

The `page_m2_fit` in `frozen` state.
