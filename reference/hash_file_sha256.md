# Compute a SHA-256 file fingerprint

Compute a SHA-256 file fingerprint

## Usage

``` r
hash_file_sha256(path)
```

## Arguments

- path:

  Path to a regular source artifact file.

## Value

A lowercase SHA-256 hash string.
