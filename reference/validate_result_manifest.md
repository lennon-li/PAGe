# Validate a disclosure-safe result manifest

Validate a disclosure-safe result manifest

## Usage

``` r
validate_result_manifest(manifest)
```

## Arguments

- manifest:

  A manifest created by \[new_result_manifest()\].

## Value

\`TRUE\` if \`manifest\` is valid; otherwise an informative error.
